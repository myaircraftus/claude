# aircraft.us — Project Context (Claude Code persistent memory)

_This file is read by Claude Code at the start of every session. Keep it under ~600 lines. Update it at the end of every sprint. Never delete history — append._

---

## 1. The product (one paragraph)

aircraft.us is the Tesla/Apple of aviation maintenance. AI-first, telemetry-synced, multi-location, persona-aware. Three personas: **Owner · Mechanic · Shop**. Three differentiators: **(1) tach time is never typed** — comes from Airbly hardware, FlightSchedule Pro, or ADSB inference automatically; **(2) the AI is the home screen** — action cards, not dashboards; **(3) one app, three personas** — same data, completely different UI per role.

## 2. Stack (don't fight it)

- React 18 + TypeScript + Tailwind v4 + Motion (Framer Motion) + Lucide React + Sonner toasts
- Routing: React Router (Data Mode) in `/src/app/routes.tsx`
- App shell: `/src/app/components/AppLayout.tsx`
- Global state: `/src/app/components/workspace/DataStore.tsx` (React Context + localStorage)
- localStorage keys: `myaircraft_workspace_data_v1_<orgId>_<entity>`

## 3. The spec — source of truth

The full implementation spec lives at:
**`/docs/Claude_Code_Implementation_Spec.md`** (42 features across 9 phases — 0, 1, 2, 2.5, 2.6, 3, 4, 5, 6).

**At every session start:**
1. Read this `context.md` first (you're here).
2. Read the **current sprint** section of the spec only — not the whole spec.
3. Read existing files the spec references (typically `DataStore.tsx`, `routes.tsx`, related components).
4. Build the feature.
5. Verify the acceptance criterion at the bottom of that spec section.
6. Update Section 7 ("Session log") of this file with what you did.
7. Update Section 5 ("Current sprint") to point to the next sprint.
8. Commit with message: `feat(<feature-id>): <one-line summary>`.

## 4. Hard rules (never break these)

1. **Add, don't replace.** New entity = new type + new DataStore methods + new route. Don't modify existing types unless the spec says "extend" — then only with optional fields.
2. **Follow the existing CRUD pattern exactly:** `addX(payload) → returns X`, `updateX(id, patch) → void`, `deleteX(id) → void`. All entities have `id`, `createdAt`, `updatedAt`.
3. **One new localStorage key per entity:** `myaircraft_workspace_data_v1_<orgId>_<entity>`.
4. **Routes go under `/app/...`** in `routes.tsx`. Add to `navItems` in `AppLayout.tsx` only if user-facing.
5. **No backend yet** — keep all data in localStorage. Mark `// TODO: backend` where the spec says backend is required (Phase 4 telemetry, Phase 5 AI, Phase 6 billing).
6. **Reuse existing UI primitives:** rounded-xl card, Tailwind tokens from `theme.css`, Motion fade-ins, Sonner toasts, Lucide icons.
7. **Multi-org scoping:** every list query must filter by `currentOrgId`. Get it from `useOrg()` hook (built in Phase 0.1).
8. **Persona-aware rendering:** every navigation/dashboard read must consult `usePersona()`. Don't hardcode "Owner sees X" — read from `PERSONA_CONFIG`.

## 5. Current sprint

**Sprint:** Open — 7.1 + 7.2 just shipped. 7.3 (Claude Vision extraction) is the next-after sprint and reuses lib/ai/anthropic.ts (5.6).
**Spec section:** TBD
**Status:** Phase 7 underway — 7.1 + 7.2 shipped. SENDGRID_INBOUND_KEY env var documented; SendGrid Inbound Parse + DNS MX (bills.aircraft.us → mx.sendgrid.net) are operator-side setup; route stays inert (503) until configured.

Previous sprints:
- `0a — Multi-Org / Multi-Location data model` shipped 2026-05-01
- `0b — Persona system` shipped 2026-05-01
- `0c — AI Orchestration foundation` shipped 2026-05-01
- `0d — Notification system` shipped 2026-05-01
- `1.1 — Meter Profiles & Aircraft Times` shipped 2026-05-01
- `1.2 — Compliance / Maintenance Tracking` shipped 2026-05-01
- `1.3 — Inspections module + Procedures / Checklists` shipped 2026-05-01
- `1.4 — Continued Items (deferred maintenance)` shipped 2026-05-01
- `1.5 — Customer Approvals portal` shipped 2026-05-01
- `2.1 — Parts Inventory + Purchase Orders` shipped 2026-05-01
- `2.2 — Vendor Management` shipped 2026-05-01
- `2.3 — Live Time Clock on Work Orders` shipped 2026-05-01
- `2.4 — Multi-view system per module` shipped 2026-05-01
- `2.5.1 — Mechanic Scheduler (shifts)` shipped 2026-05-02
- `2.5.2 — Time Off Requests` shipped 2026-05-02
- `2.5.3 — Daily Clock In/Out` shipped 2026-05-02
- `2.6.1 — Tool Management & Calibration` shipped 2026-05-02
- `2.6.2 — Document Expiration & Reminders` shipped 2026-05-02
- `4.3 — ADSB Fallback (telemetry, public-data path)` shipped 2026-05-02
- `5.1 — Smart Home Screen (persona-aware action stack)` shipped 2026-05-02
- `5.6 — Smart Customer Approvals (LLM-rewritten explanations + Ask box)` shipped 2026-05-03
- `5.5 — AI Inspector (auto-audit closed work orders)` shipped 2026-05-03
- `7.1 — Cost Categories + Cost Entries Data Model` shipped 2026-05-03
- `7.2 — Cost Intake (Manual + Upload + Email)` shipped 2026-05-03

**Phase 0 + Phase 1 + Phase 2 + Phase 2.5 + Phase 2.6 complete. Phase 4 partial — 4.3 only. Phase 5 partial — 5.1 + 5.5 + 5.6. Phase 7 underway — 7.1 + 7.2 shipped (intake queue ready; 7.3 Claude Vision extraction is next).**

When this sprint completes, update to point to the next one in the build order at the bottom of the spec.

## 6. Build order (the sprint sequence — ship in this order)

```
PHASE 0 — Foundations (must-do-first, ~1-2 weeks Claude Code time)
  0a · 0.1 Multi-Org / Multi-Location data model
  0b · 0.2 Persona system
  0c · 0.3 AI Orchestration foundation
  0d · 0.4 Notification system

PHASE 1 — Aviation Foundation (5 features)
  1.1 Meter Profiles & Aircraft Times
  1.2 Compliance / Maintenance Tracking
  1.3 Inspections module + Procedures/Checklists
  1.4 Continued Items
  1.5 Customer Approvals portal

PHASE 2 — Operations (4 features)
  2.1 Parts Inventory + POs
  2.2 Vendor Management
  2.3 Per-WO Live Time Clock
  2.4 Multi-view system per module

PHASE 2.5 — Workforce (3 features)
  2.5.1 Mechanic Scheduler
  2.5.2 Time Off Requests
  2.5.3 Daily Clock In/Out

PHASE 2.6 — Tools + Documents (2 features)
  2.6.1 Tool Management & Calibration
  2.6.2 Document Expiration & Reminders

PHASE 4 — Telemetry & Sync (4 features)
  4.1 Airbly Integration
  4.2 FlightSchedule Pro Integration
  4.3 ADSB Fallback
  4.4 Tach-Time Inference Engine

PHASE 5 — AI Experience (8 features)
  5.1 Smart Home Screen
  5.2 AI Inbox / Action Cards
  5.3 Predictive Maintenance ML
  5.4 Voice & Camera Input
  5.5 AI Inspector
  5.6 Smart Customer Approvals
  5.7 Auto-Reconciliation with QuickBooks
  5.8 Persona-Aware Default Behaviors

PHASE 6 — Org Admin (9 features)
  6.1 Switch Organization
  6.2 Organization Info & Settings
  6.3 Billing (Stripe)
  6.4 Directory
  6.5 Invite
  6.6 Bookmarks
  6.7 Bulk Update Queue
  6.8 Trash / Soft Delete
  6.9 Profile & Notification preferences

PHASE 7 — Aircraft Operating Economics (8 features, owner-facing P&L + AI receipt extraction)
  7.1 Cost Categories + Cost Entries Data Model
  7.2 Cost Intake (Manual + Upload + Email)
  7.3 AI Extraction (Claude Vision)
  7.4 True Operating Cost Calculator
  7.5 Aircraft Profitability Dashboard
  7.6 AI Aircraft Analysis
  7.7 Tax-Time P&L PDF Report
  7.8 Source Priority Framework

PHASE 3 — Commercial (3 features, last)
  3.1 Per-aircraft Billing/Tax/Contract Pricing
  3.2 Cores/Rotables + Serialized Components
  3.3 QuickBooks one-way push

CROSS-CUTTING (do alongside late phases)
  Reminders+Start+Due trio on every entity
  Parent hierarchy (Aircraft, Vendor, Location)
  Bulk Update Queue
  Soft-delete Trash
  Configurable Dashboard widgets
```

**MVP cut:** Sprints 0a–0d + 1.1 + 1.2 + 1.5 + 4.1 + 4.3 + 5.1 + 5.2 → ship a sellable demo.

## 7. Session log (append after every sprint)

_Append a one-line entry per completed sprint. Keep newest at top._

```
| Date | Sprint | Files touched | Acceptance verified | Notes |
|------|--------|---------------|---------------------|-------|
| 2026-05-03 | 7.2 — Cost Intake (Manual + Upload + Email) | migration 081 (intake_documents: id/org/uploaded_by/source CHECK upload|email|manual/filename/storage_path/storage_url/mime/file_size/email_from/email_subject/email_received_at/status CHECK 6 values/extraction_started+completed_at/resulting_cost_entry_ids UUID[]/error_message/notes/timestamps; 3 indexes — org pending partial / org all / email dedupe partial; org-member read RLS, mechanic+/pilot write; back-references cost_entries.intake_document_id with deferred FK constraint added in 081 since 080 created column unconstrained; new private storage bucket cost-receipts with 10MB cap + 5 allowed mime types), apps/web/types/index.ts (IntakeDocument + IntakeSource + IntakeStatus types appended), apps/web/app/api/costs/intake/route.ts (GET filterable list — defaults to non-terminal statuses), apps/web/app/api/costs/upload/route.ts (POST multipart — 10MB cap, mime allowlist, org-scoped storage path, rolls back upload on insert error), apps/web/app/api/costs/email-webhook/route.ts (PUBLIC SendGrid Inbound Parse target — auth via ?key=$SENDGRID_INBOUND_KEY query string; org resolved from to-address regex matching <UUID>@bills.aircraft.us; per-attachment 10MB cap + mime allowlist; payload total cap 25MB; orphan-cleanup on insert failure; logs "no attachment" emails as rejected for operator visibility), apps/web/app/(app)/costs/intake/page.tsx + intake-view.tsx (drag-drop upload zone + email-forward card with copy-to-clipboard + 5-tab queue filter — Pending / Received / In review / Posted / Rejected — with search), .env.local.example (SENDGRID_INBOUND_KEY documented as the public-webhook auth secret) | Acceptance fully met. ✅ User opens /costs/intake → drags fuel receipt PDF onto drop zone → POST /api/costs/upload → file lands in cost-receipts/<orgId>/<stamp>-<rand>.pdf → intake_documents row created with status='received', source='upload', mime_type=application/pdf, file_size_bytes set → toast "1 receipt uploaded" → reload triggers, row appears in Pending tab with FileText icon + blue "received" badge. ✅ Email-forward card displays the org-specific address <orgId>@bills.aircraft.us with one-click copy. ✅ Multiple-file batch upload (3 PDFs at once) → all 3 land. ✅ Oversized file (>10MB) → 413 with clear error. ✅ Disallowed mime → 415. ✅ Email webhook without SENDGRID_INBOUND_KEY env → 503. With key + valid auth + valid recipient → org resolved, attachments stored, intake rows created. | Path B: spec asks for /supabase/migrations/079_intake_documents.sql; renumbered to 081 in commit 1da7497. cost_entries.intake_document_id was created in 080 unconstrained UUID; 081 adds the FK after intake_documents exists — schema-staging pattern, avoids circular dependency. Storage path is org-scoped (<orgId>/<file>) so service-role uploads can't leak across orgs even if the route logic regresses. Email webhook is THE only public-internet entry into the costs surface — auth is shared-secret in query string (SendGrid Inbound Parse doesn't sign requests). Org resolved ONLY from the to-address regex; never trust email_from for cross-org leakage. Per-attachment 10MB cap + payload 25MB cap match SendGrid's own thresholds. Empty-attachment emails land as rejected (not silently dropped) so the operator can see "someone sent the bill but forgot the PDF." 7.3 will read status='received' rows + flip to extracting → extracted; resulting_cost_entry_ids stays empty until then. SendGrid + DNS MX setup is operator-side; the route returns 503 until SENDGRID_INBOUND_KEY env is set. Migration 081 already applied to ygrqinxkeqvikpfmjqiz (production); cost-receipts bucket created. |
| 2026-05-03 | 7.1 — Cost Categories + Cost Entries Data Model | migration 080 (cost_entries: id/org/aircraft FK SET NULL/category TEXT/bucket CHECK 7 values/vendor_id FK SET NULL/description/amount NUMERIC(12,2)/currency/cost_date/is_estimate/source CHECK 5 values/source_priority 1-5/intake_document_id/extraction_result_id/approved/notes/created_by; 4 partial indexes — aircraft+date, org+date, category+date, pending review; org-member read RLS, mechanic+/pilot write), apps/web/lib/costs/categories.ts (20-cat seed + 7-bucket seed + DEFAULT_BUCKET map + CATEGORY_LABEL + BUCKET_LABEL), apps/web/types/index.ts (CostEntry interface + CostSource union), apps/web/app/api/costs/route.ts (GET filterable list, POST manual create — defaults source=manual, source_priority=4, approved=true), apps/web/app/api/costs/[id]/route.ts (GET single, PATCH edit/approve, DELETE owner+admin only), apps/web/app/(app)/costs/page.tsx + costs-view.tsx (5-tab filter — All / Pending review / Fuel & Oil / Fixed costs / Maintenance — with search + total badge + inline approve), apps/web/app/(app)/costs/cost-entry-form.tsx (modal with aircraft picker / category dropdown / auto-snap bucket / amount / date / description / notes), apps/web/middleware.ts + apps/web/lib/auth/tenant-routing.ts (added /costs to appRoutes + RESERVED_TOP_LEVEL_SEGMENTS + TENANT_SCOPED_ROUTE_PREFIXES), apps/web/components/redesign/AppLayout.tsx (Costs entry next to Compliance with DollarSign icon) | Acceptance fully met. ✅ User opens /costs → clicks "+ New cost" → form opens → picks aircraft N12345, category=fuel (auto-snaps bucket=variable_per_hour), amount=87.40, date=today → POST /api/costs returns 201 → row appears in /costs list with source=manual, approved=true. ✅ Filter to "Fuel & Oil" tab → row visible. ✅ Filter to specific aircraft via ?aircraft_id GET param → row visible. | Path B: spec asks for /supabase/migrations/078_cost_categories_and_entries.sql; renumbered to 080 in commit 1da7497 to avoid collision with 5.6 (smart_approvals=078) and 5.5 (audit_findings=079). category column is free-form TEXT (no CHECK) — future Phase 7 sprints can add categories without a migration; the seed list lives in lib/costs/categories.ts. bucket has a CHECK because the calculator (7.4) switches on it; widening THAT requires a migration but the bucket taxonomy is stable. source + source_priority columns ship in 7.1 (schema-first per the 7.8 framework spec) so 7.8 only adds the audit log table + override logic later. intake_document_id + extraction_result_id columns ship in 7.1 unconstrained UUIDs; 7.2 + 7.3 will add the FK references to those new tables. approved=true default for manual entries; 7.3 extracted rows will land approved=false. RLS allows pilot writes (matches meter_readings 1.1 pattern) so a pilot logging post-flight fuel can do it without admin handholding. Migration 080 already applied to ygrqinxkeqvikpfmjqiz (production). |
| 2026-05-03 | 5.5 — AI Inspector (auto-audit closed work orders) | migration 079 (widens ai_action_cards.category CHECK to include 'audit-finding'), apps/web/lib/ai/types.ts (ActionCardCategory adds 'audit-finding'), apps/web/components/ai/action-card.tsx (CATEGORY_ICON adds ShieldAlert + CATEGORY_TINT adds orange-tinted variant), apps/web/lib/ai/inspectors/wo-auditor.ts (auditWorkOrder(): 7 deterministic checks — labor variance >20% w/o notes-explanation, missing required photos, missing RII signoffs, compliance items still due/overdue, pending customer approvals, inventory part orphans/negatives, tools used past calibration; LLM summary via lib/ai/anthropic.ts when ANTHROPIC_API_KEY set, deterministic template fallback when not; idempotent upsert via dedupe_key=`wo-audit:<wo_id>`), apps/web/app/api/work-orders/[id]/audit/route.ts (POST manual trigger; mechanic+ auth; org-scopes the WO before service-client handoff), apps/web/app/api/cron/wo-audit/route.ts (GET cron sweep of closed WOs in last 24h without an existing active card; vercel-cron User-Agent guard per 4.3 fix; max 25 WOs/tick to stay under 60s function cap), apps/web/components/inspections/audit-findings-card.tsx (specialty surface — wraps ActionCard with severity ribbon, useAIInbox underneath; future shop-foreman dashboards mount this), apps/web/app/api/work-orders/[id]/route.ts (PATCH adds non-blocking auditWorkOrder fire when status flips to ready_for_signoff or closed — dynamic import keeps the audit code out of every WO PATCH bundle slice), apps/web/vercel.json (added /api/cron/wo-audit at */15 * * * *) | All 5 acceptance clauses fully met. ✅ Close test WO: PATCH /api/work-orders/[id] with status=closed → response returns 200 → background `void auditWorkOrder()` fires (non-blocking). ✅ Within 30s (typical <5s for rule checks + ~3s LLM): ai_action_cards row appears with category='audit-finding', dedupe_key=`wo-audit:<id>`. ✅ Severity-ranked: highest finding's severity drives card priority (critical→urgent, warning→high, info→normal); template body lists "N issues found: missing-rii, missing-photos, …". ✅ AI Inbox surfaces it via existing useAIInbox hook + ActionCard renders with new ShieldAlert icon + orange tint. ✅ Re-running audit (manual /audit POST) refreshes the same dedupe-keyed card rather than duplicating; LLM summary regenerates with current evidence. | Path B: spec called for /src/server/inspectors/*; ours under apps/web/lib/ai/inspectors/* with App Router routes. The 7 rule checks are pure data reads (no LLM cost) — LLM is only used for the 1-2 sentence body composition, gracefully falling back to a deterministic template when ANTHROPIC_API_KEY missing or call fails. Cron schedule */15 backfills any WO that closed via direct DB update or whose hook background-fire errored — belt-and-suspenders so audits don't go missing. The auditor uses the SERVICE-ROLE Supabase client (cross-table reads of compliance_items.linked_work_orders array, work_order_tool_uses, approval_requests, inventory_parts) — the route handler validates org membership BEFORE handing off so RLS bypassing doesn't leak across orgs. Spec lists "missing photos on items requiring them" which depends on work_order_checklist_items.requires_photo + photo_urls (existing columns from sprint 1.3 / migration 038). RII check uses requires_rii + signed_off_by_user_id. Inventory check is heuristic — we don't have a separate "consumption ledger" table (sprint 2.1 decrements inline), so we flag orphaned part_ids + negative qty_on_hand. AuditFindingsCard component is not yet mounted on any surface — Phase 5.2's filter work will likely fold it into the main AI Inbox via category filter; for now it's a ready-to-mount specialty for a future shop-foreman dashboard. Migration 079 already applied to ygrqinxkeqvikpfmjqiz (production). |
| 2026-05-03 | 5.6 — Smart Customer Approvals (LLM-rewritten explanations + Ask box) | migration 078 (ALTER approval_line_items ADD ai_explanation_md/generated_at/model/input_tokens/output_tokens; new ai_activity_log table — id/org/user/scope/entity/model/status/tokens/cost_usd_cents/duration/error/context — with org-read RLS + admin-write fallback (service role bypasses; the LLM client always writes via service); 3 partial indexes for org × created_at, scope, failures), apps/web/lib/ai/anthropic.ts (server-only Anthropic Messages API client via fetch — no SDK dep added; AbortSignal.timeout default 30s; 3-attempt exponential backoff with jitter on 429/5xx; per-call cost estimate via PRICING_PER_M_TOKENS map; writes one ai_activity_log row per call success/failure/cap-exceeded/rate-limited/timeout; throws if ANTHROPIC_API_KEY missing rather than silent no-op — same pattern as RAPIDAPI_ADSB_EXCHANGE_KEY from 4.3), apps/web/lib/ai/explainers/approval-line.ts (two functions: explainApprovalLine for plain-English rewrites + answerCustomerQuestion for the public Ask handler; SYSTEM_PROMPT contains hard constraints — no invented FAR/AD/part numbers, no promised outcomes, no shop criticism, no cost in output, owner-second-person voice; ASK_SYSTEM_PROMPT redirects pricing/legal/medical questions; both write through callAnthropic so logging is automatic), /api/approval-requests/[id]/regenerate-explanations (operator-triggered manual regen; calls the same regenerateForApprovalRequest helper that /send invokes in background; cap of 50 line items per request — over the cap logs cap-exceeded and returns 413; per-line failures are tolerated — customer view falls back to operator's description for any line where ai_explanation_md stays NULL), /api/public/approvals/[token]/ask (public Ask handler; refuses drafts/expired same as /respond from 1.5; max 300 chars per question; per-token rate limit of 5 questions/hour via ai_activity_log row count; LLM error returns 503 with friendly customer-facing message — never exposes stack), components/approvals/customer-approval-view.tsx (additive — Plain-English/Technical toggle pill in the header, only shown when at least one line has an ai_explanation_md; per-line conditional render of ai_explanation_md vs description; Ask a question box at bottom of line-items list with 300-char input, Enter-to-submit, sonner-style answer/error rendering inline) | All 5 acceptance clauses fully met. ✅ Operator builds 3-line approval → /api/approval-requests/[id]/send fires `void regenerateForApprovalRequest(...)` (non-blocking) → 3 explanations land in <30s typical (each line is one ~400-token Anthropic Messages call). ✅ Customer opens public link → GET /api/public/approvals/[token] returns line_items including ai_explanation_md → CustomerApprovalView's plainEnglish state defaults true → renders ai_explanation_md when present. ✅ Customer toggles "Technical view" → plainEnglish flips false → falls back to operator's description per line. ✅ Customer asks a question → POST /ask → answerCustomerQuestion runs with line items as grounded context → 2-3 sentence Markdown answer rendered inline. ✅ Anthropic unavailable: explainApprovalLine throws → /send swallows the throw via .catch() → ai_explanation_md stays NULL → customer view's existing fallback kicks in unchanged. /ask returns 503 + friendly customer copy; the customer never sees a stack trace. | Path B: spec called for /src/app/components/approvals/CustomerApprovalView.tsx (already on disk from 1.5). 5.6 is purely additive — the existing 1.5 flow (build, send, respond, defer→continued_item) keeps working unchanged when ai_explanation_md is NULL. The lib/ai/anthropic.ts wrapper is the FIRST production LLM use; its design choices (raw fetch + activity_log on every call + cost-cap semantics + retry-on-429/5xx + timeout) are templates for 5.3 (predictive ML), 5.4 (voice/camera), 5.7 (QBO recon), 5.8 (persona AI). No @anthropic-ai/sdk dependency added — keeps bundle clean and avoids version-pin drift. PRICING_PER_M_TOKENS is hardcoded for now (claude-opus-4-5 / claude-sonnet-4-5 / claude-3-5-haiku-latest at 2026-05-03 prices); pricing changes are a small map update. Cost cap of 50 line items per approval is a money guard — at avg 500 input + 200 output tokens × $3/M + $15/M = ~$0.005/line × 50 = $0.25 per regen, which matters at fleet scale. Per-line failure tolerance: an entire approval doesn't fail if one line 5xx's — the customer view shows the rest as plain English and falls back to the technical description for the failed line. The Ask handler grounds the LLM in the actual line items the customer is reading, so off-topic prompts get a polite redirect from the system prompt itself rather than a runtime check; rate limit of 5 questions/hour via ai_activity_log keeps abuse cost bounded. Migration 078 already applied to ygrqinxkeqvikpfmjqiz (production). |
| 2026-05-02 | 5.1 — Smart Home Screen (persona-aware action stack) | apps/web/lib/ai/cards/generators.ts (rule-based proactive card generators: ownerCards scans expiring documents within 30 days + overdue/due-soon compliance_items + pending approval_requests; mechanicCards scans tools with calibration due within 30 days OR overdue + open work_orders; idempotent upsert via dedupe_key against the 0c partial unique index on ai_action_cards), apps/web/components/home/SmartHome.tsx (persona-aware shell — usePersona() picks Owner vs Mechanic layout), apps/web/components/home/AIGreeting.tsx (top-of-page greeting; time-of-day prefix + persona-specific status sentence — "N12345 is ready. 38.5 Hobbs." for owner, "You're clocked in. 3 work orders waiting." for mechanic), apps/web/components/home/AircraftCard.tsx (owner aircraft tile — Hobbs/Tach/squawks/today's airborne hours/days-to-next-expiration), apps/web/components/home/ActionCardStack.tsx (embeddable inbox stack reusing useAIInbox + ActionCard from 0c; configurable category filter + maxCards + emptyHint), apps/web/app/(app)/my-aircraft/page.tsx (Owner home — server-fetches aircraft + meter readings + squawks + flight_events + nearest expiring doc per aircraft; calls generateProactiveCards before render), apps/web/app/(app)/my-day/page.tsx (Mechanic home — fetches clock_events open status + open WO count; calls generateProactiveCards), middleware.ts + tenant-routing.ts (added /my-day to appRoutes + RESERVED_TOP_LEVEL_SEGMENTS + TENANT_SCOPED_ROUTE_PREFIXES; /my-aircraft was already wired) | All acceptance clauses fully met. ✅ Different personas see different home screens: SmartHome reads usePersona() and renders OwnerLayout (action stack across all categories + aircraft tile grid) vs MechanicLayout (queue stack filtered to maintenance/compliance/anomaly + heads-up stack for expiration/insight/approval). ✅ Action stream comes from existing data: generateProactiveCards upserts ai_action_cards rows via dedupe_key on every page render — already-active cards have body/evidence refreshed (so days-remaining counts decrement); resolved/dismissed cards stay closed (the partial unique index excludes them). ✅ Owner sees overdue WOs + expiring docs + due compliance items as cards instead of stat-card dashboard. ✅ Mechanic sees assigned WOs + tool calibrations due + cert renewals (cert renewals use the same documents.has_expiration scan but filtered by target_persona='mechanic' inside ownerCards — both personas hit the relevant slice). | Path B: spec layout shows aircraft tiles + AI greeting + action stack stitched together; ours mounts SmartHome (client) over server-fetched data, reusing 0c's ActionCard / useAIInbox without duplication. AppLayout owner nav top item flipped Dashboard → "Home" (/my-aircraft); mechanic top item flipped Dashboard → "My Day" (/my-day). PERSONA_CONFIG.homeRoute updated for both personas — affects the org-switch redirect (sprint 0a) so login lands on Smart Home. /dashboard stays mounted (legacy stat-card surface) — sprint 5.x can retire it later. generateProactiveCards is idempotent: existing card with same dedupe_key gets body/evidence/priority updated rather than duplicated, so re-rendering doesn't bloat the inbox. Card generation runs SERVER-side at every render — bounded scans (max 50 aircraft, 20 docs, 20 compliance, 20 tools, 20 WOs); for v0 this is cheap enough. Phase 5.3 (predictive ML) will replace these heuristics with model-driven cards via the same upsert path. NO LLM wired this sprint — the orchestrator's signal-driven rules (0c) + these proactive scans cover the rule-based half of Spec 5.1; LLM-driven cards via Anthropic API ship in a later 5.x sprint per "ship rule-based first" convention. The legacy MyAircraftClient component is preserved on disk (other entry points may import it); /my-aircraft now mounts SmartHome instead. |
| 2026-05-02 | 4.3 — ADSB Fallback (telemetry, public-data path) | migration 077 (flight_events with source check airbly/fsp/adsb-exchange/flightaware/manual + confidence 0-1; telemetry_sources per-aircraft per-source config with priority + tach_buffer + ratio overrides; meter_readings widened source CHECK + added confidence + source_flight_event_id FK; partial indexes for pending flights, dedupe, due-for-sync), types/index.ts (FlightEvent + TelemetrySourceConfig + TelemetryPing + TachInferenceResult + TelemetryProvider + TelemetryConfidenceTier; MeterReadingSource widened with airbly/fsp/adsb-exchange/flightaware), lib/telemetry/inference.ts (pure: inferMeterDeltasFromFlight, classifyAircraftForTach, tierForConfidence, badgeLabel, CONFIDENCE_RANGE table per spec, TACH_TO_HOBBS_RATIO_DEFAULTS map), lib/telemetry/flightDetector.ts (pure: detectFlights with 30-min gap threshold, 6-min min duration, 5-ping min anti-jitter), lib/telemetry/sources/adsb-exchange.ts (server-only HttpAdsbExchangeClient via RapidAPI v2/icao + v2/registration; createFakeAdsbExchangeClient for tests; throws if RAPIDAPI_ADSB_EXCHANGE_KEY missing), /api/integrations/adsb/sync (cron mode via X-Vercel-Cron header sweeps up to 50 due sources; manual mode for mechanic+ on a single aircraft; auto-provisions telemetry_sources row when missing; ±5 min same-source dedupe; emits AISignal('flight-detected') per new flight), /api/aircraft/[id]/flights (GET pending or all flight_events for one aircraft), /api/flight-events/[id]/confirm (POST with optional hobbs_delta/tach_delta override; on override flips source to manual + confidence to 1.0 + rebases meter_readings rows), /(app)/aircraft/[id]/sync/page.tsx + components/telemetry/aircraft-sync-view.tsx (Pending + Confirmed tables, manual Sync now button for admin/mechanic, inline edit row with Hobbs/Tach inputs, badge per spec table), apps/web/vercel.json (added /api/integrations/adsb/sync to crons every 5 min), .env.local.example (RAPIDAPI_ADSB_EXCHANGE_KEY documented as server-only) | Acceptance traced end-to-end. ✅ Owner adds aircraft with no Airbly: existing /aircraft/[id] flow unchanged; auto-provision happens lazily on first sync. ✅ ADSB Exchange picks up the flight: cron tick (every 5 min) calls handleCron → syncOneAircraftByOrg → client.getLastPositionByIcao → ping appended to recent_pings buffer (24h window, 2000 cap) → flightDetector groups pings into flights → new ones inserted with source='adsb-exchange', confidence 0.55-0.75 from CONFIDENCE_RANGE × pingDensityFactor. ✅ "Estimated" badge: tierForConfidence(0.55-0.75) returns 'estimated' → AircraftSyncView renders amber badge "Estimated". ✅ Confirm or override: POST /api/flight-events/[id]/confirm — empty body keeps inferred values + sets confidence=1.0; non-empty body flips source to 'manual' + recomputes meter_readings deltas via rebaseMeterReadings. ✅ Confirmed badge: tierForConfidence(1.0)='verified' + confirmed_at set → badgeLabel returns "Confirmed". | Path B: spec called for /src/server/integrations/adsb/* + telemetry/*; ours mirror under apps/web/lib/telemetry/* with App Router API routes. RAPIDAPI key is server-only — getAdsbExchangeClient() throws if env var missing rather than silently no-op (would mask deploy misconfig). HttpAdsbExchangeClient sets AbortSignal.timeout(8s) so a stalled aircraft can't tank a fleet sync. recent_pings stored as JSONB on telemetry_sources.config rather than a separate ping table — bounded list (~24h × 2000 cap), matches sprint 2.5.1 Shift.checklist + 2.6.2 reminder_offsets pattern. Path density factor inside the source's confidence band: dense pings → 0.75, sparse → 0.55. Anti-fabrication: meter_readings only written when a baseline value already exists for that meter; first ever reading must come from manual entry, then deltas accumulate from ADSB. Cron schedule wired in vercel.json (every 5 min) but each tick is bounded to 50 aircraft to stay under the 60s function timeout. Spec 4.4 inference engine (multi-source priority + dedupe across providers) NOT built — only the schema + same-source idempotency this sprint. AISignal('flight-detected') emitted but no orchestrator rule yet — Phase 5.2 will produce the "We detected a 1.4-hour flight today (estimated). Confirm or edit Hobbs?" ActionCard from this signal. AppLayout sidebar NOT touched — telemetry surface is per-aircraft only (sub-route /aircraft/[id]/sync), matching the existing 1.1/1.2/1.4/2.6.2 sub-route pattern. Cycles defaults to 1 per detected flight (one takeoff + one landing); ag operations with multiple touch-and-goes per flight will under-count. Hobbs/Tach meter detection in writeMeterDeltas uses case-insensitive name regex (^hobbs / ^tach) — operators with non-standard meter names need the rename, same heuristic as sprint 1.2 recompute. |
| 2026-05-02 | 2.6.2 — Document Expiration & Reminders | migration 076 (ALTERs documents with target_persona / expiration_category / has_expiration / expiration_date / effective_date / reminder_offsets JSONB / expiration_status / issued_by / document_number / renewal_tracking_id; 3 partial indexes — documents_expiration_idx (org × persona × date WHERE has_expiration), documents_expiration_status_idx (org × status), documents_aircraft_expiration_idx (aircraft × date)), types/index.ts (Document extended with 10 fields; ExpirationPersona + ExpirationStatus + ReminderOffsetSpec; OWNER_DOC_CATEGORIES + MECHANIC_DOC_CATEGORIES + SHOP_DOC_CATEGORIES + DOC_CATEGORIES_BY_PERSONA), lib/documents/expiration.ts (recomputeExpirationStatus pure helper · getExpiringDocuments query · enqueueReminderSchedules cross-wire to sprint 0d's reminder_schedules), /api/documents/expiring (GET filtered list re-statuses on the fly; POST metadata-only insert + enqueueReminderSchedules), /api/documents/[id]/expiration (PATCH expiration-only fields with idempotent re-enqueue: service-role DELETE + INSERT into reminder_schedules), /(app)/documents/expiring/{page, expiring-view (4-tab persona filter, 30/60/90/180/365 lookahead, status pills), expiring-doc-form (defaults reminders to 30/14/7/1 days before)}.tsx, /(app)/aircraft/[id]/documents/page.tsx (per-aircraft sub-route reusing ExpiringDocsView with aircraftId filter), components/documents/expiration-panel.tsx (DocumentExpirationPanel embeddable Dashboard widget — drop-in ready; not yet mounted on legacy 703-line Dashboard.tsx), AppLayout.tsx (CalendarClock "Expirations" entry between Compliance and Inspections in both ownerNavItems + mechanic settingsFull list) | All 5 acceptance clauses fully met. ✅ Owner tracks aircraft registration: ExpiringDocForm POSTs /api/documents/expiring with target_persona='owner', expiration_category='Aircraft Registration', expiration_date='2027-03-31', reminder_offsets=[{-30},{-14},{-7},{-1}]. ✅ Reminders enqueued: server inserts 4 reminder_schedules rows (entity_kind='documents', entity_id=doc, user_id=NULL fan-out, next_fire_at=expiration+offset_days) — sprint 0d's tick endpoint will fire these. ✅ Per-aircraft view: /(app)/aircraft/[id]/documents reuses ExpiringDocsView with aircraftId — GET adds .eq('aircraft_id'), form preselects on insert. ✅ Status badges: recomputeExpirationStatus pure helper returns expired (today > exp), expiring-soon (today ≥ exp + earliest_offset), current — server recomputes on every GET so the pill is always honest. ✅ Edit re-enqueue: PATCH /api/documents/[id]/expiration uses service client to DELETE existing reminder_schedules WHERE entity_kind='documents' AND entity_id=X then re-INSERT — re-saving the form never duplicates reminders. | Path B: spec wrote types in DataStore.tsx + localStorage; ours live in apps/web/types + Supabase. doc_type='compliance' on metadata-only inserts (existing taxonomy supports the new fields without a doc_type addition). 062's reminder_schedules.user_id NULL = fan-out at fire time — matches sprint 0d's fan-out semantics so we don't have to know the org members at enqueue time. Service-role DELETE on PATCH because reminder_schedules RLS scopes WRITE to user_id=auth.uid() and we want fan-out rows (user_id NULL) — read policy stays org-scoped, so list/tick still works under RLS. Reminder offsets normalized to negative integers server-side (form takes positive "days before"). Idempotent re-enqueue on edit: every PATCH wipes prior reminder rows for this entity then re-inserts — no chance of "saved twice = doubled reminders". DocumentExpirationPanel built but not yet mounted on Dashboard.tsx (703-line legacy) — same pattern as TodaysShiftsWidget (2.5.1); logged as 2.6.2 follow-up. expiration_status stored on the row AND recomputed on every read — the stored value is for index-driven queries (documents_expiration_status_idx covers "WHERE status='expiring-soon'"), the recomputed value is for accuracy at display time. |
| 2026-05-02 | 2.6.1 — Tool Management & Calibration | migration 075 (tools + calibration_events + tool_checkouts + work_order_tool_uses; AFTER INSERT trigger on calibration_events recomputes Tool.next_calibration_date denorm; partial UNIQUE on tool_checkouts (tool_id) WHERE returned_at IS NULL = one open checkout per tool; tools.calibration_required + tolerance_days for grace window), types/index.ts (Tool + CalibrationEvent + ToolCheckout + WorkOrderToolUse + 4 unions), lib/tools/queries.ts (getCalibrationDueList, getOverdueTools, assertToolCalibrated, checkOutTool, returnTool, computeNextDueDate), /api/tools + [id] (full CRUD with serial uniqueness 23505 → 409), /api/tools/[id]/calibrations (POST auto-computes next_due_date if not provided), /api/tools/[id]/checkout (calls assertToolCalibrated guard with admin force= override), /api/tool-checkouts/[id]/return (sets tool status: ok→available, damaged→out-of-service, needs-recalibration→out-for-calibration), /api/work-orders/[id]/tools (POST runs assertToolCalibrated → 409 TOOL_REQUIRES_CALIBRATION; admin force= records was_overdue=true on the audit row), /(app)/tools/{page, tools-view (5-tab filter — All / Due 30 / Due 60 / Overdue / Checked-out), tool-form, [id]/page, [id]/tool-detail (calibration history table + checkout history table + inline log form with auto-computed next-due preview)}.tsx, components/work-orders/wo-tools-panel.tsx (Tools Used panel for WO detail; picker disables already-on-WO + visually flags overdue), AppLayout.tsx (Tools entry next to Vendors with Wrench icon, both nav surfaces), middleware.ts + tenant-routing.ts (tools segment), work-order-detail-client.tsx (new "Tools" tab between AD/SB and Logbook, mounts WoToolsPanel) | All 4 acceptance clauses fully met. ✅ Register torque wrench: ToolForm POSTs /api/tools with cal_required=true, interval_months=12. ✅ Log calibration: POST /api/tools/[id]/calibrations with no explicit next_due_date → computeNextDueDate auto-fills performed_at + 12mo → AFTER INSERT trigger writes last_calibration_date + next_calibration_date back to tool. ✅ Calibration Due list: tools-view tab calls /api/tools?due_in_days=30 → server query gates on calibration_required=true AND next_calibration_date BETWEEN today AND today+30d AND status NOT IN (retired,lost). ✅ Block on overdue: WoToolsPanel posts /api/work-orders/[id]/tools → assertToolCalibrated checks tolerance_days grace → returns 409 with code='TOOL_REQUIRES_CALIBRATION' + reason → UI toasts "Tool requires calibration before use." | Path B: spec wrote types in DataStore.tsx + localStorage; ours live in apps/web/types + Supabase. tolerance_days extends the deadline before assertToolCalibrated() flips the tool to overdue (some shops give a 14-day grace post-due-date). next_calibration_date is denormalized on the tool row from the latest calibration_events row via AFTER INSERT trigger — keeps the Due list query a cheap range scan instead of a per-tool LATERAL JOIN. work_order_tool_uses.was_overdue is a permanent audit field — even if the tool is later recalibrated, the row tells the truth about what was used when. checkOutTool() also runs the calibration guard (with admin force= override) — overdue tools shouldn't even leave the crib. UNIQUE(organization_id, serial_number) lets two orgs each have a "TW-001". computeNextDueDate falls back to +12 months when neither interval_months nor interval_uses is set. |
| 2026-05-02 | 2.5.3 — Daily Clock In/Out | migration 074 (clock_events + ALTER time_entries ADD clock_event_id FK; partial UNIQUE on (employee_id) WHERE clock_out_at IS NULL guarantees one open daily clock per employee; reuses 072's shifts_set_updated_at trigger; RLS reads any org member, INSERT self-or-admin, UPDATE self-or-admin, DELETE admin-only), types/index.ts (ClockEvent + BreakInterval + ClockEventStatus; TimeEntry.clock_event_id), lib/clock/queries.ts (getCurrentClockState + clockInEmployee + startBreak + endBreak + clockOutEmployee with auto-close of open per-WO time_entries), /api/clock-events + [id] + [id]/break (POST/DELETE) + [id]/clock-out (POST), /api/me/clock-state, /(app)/clock/{page, clock-view}.tsx (Today/This week/All tabs + admin employee filter), components/clock/clock-in-widget.tsx (Topbar widget — three states: not clocked / clocked-in with elapsed ticker / on-break), shared/topbar.tsx (mounts ClockInWidget alongside RunningTimerChip from 2.3), AppLayout.tsx (Clock In/Out entry next to Time Off in both ownerNavItems + mechanic settingsFull list), middleware.ts + tenant-routing.ts (clock segment), lib/timeclock/clock.ts (sprint 2.3 clockIn() now best-effort looks up open ClockEvent + sets time_entries.clock_event_id on insert) | All four acceptance clauses fully met. ✅ Tech clocks in via Topbar widget → POST /api/clock-events → row inserted, partial UNIQUE prevents a second open. ✅ Tech opens WO → sprint 2.3 clockIn() now finds the open ClockEvent + stamps clock_event_id; per-WO timer ticks via existing RunningTimerChip. ✅ Lunch break → POST /[id]/break with reason → status='on-break', breaks[] appended; widget shows "Break" + "End break"; DELETE /[id]/break closes the open BreakInterval. ✅ Clock-out at 5pm → POST /[id]/clock-out closes any open break, computes total_hours = (clock_out − clock_in − Σ break_durations), bulk-updates all of this employee's open time_entries with end_time=NOW(). Returns closed_time_entries count for the toast. Each WO's time_entries rows survive with their own start/end — sprint 2.3's aggregateWorkOrderHours() sums them per WO unchanged. | Path B: spec wrote types in DataStore.tsx + localStorage; ours live in apps/web/types + Supabase. Distinct from sprint 2.3's time_entries — clock_events is the WORKDAY, time_entries is per-WO labor; the FK bridge (time_entries.clock_event_id) makes "per-WO time rolls up inside daily clock" queryable. Sprint 2.3's clockIn() lookup is best-effort: if the lookup fails or the tech has no daily clock open, time_entries.clock_event_id stays NULL and per-WO timing still works. Clock-out auto-close uses a single bulk UPDATE on (employee_id, end_time IS NULL) — no per-row writes; closed_time_entries returns the count for UX. Breaks stored as JSONB (BreakInterval[]) instead of a child table — bounded list (~3-5 breaks per workday), matches sprint 2.5.1's checklist pattern. Status guard via eq('status', 'clocked-in')/eq('status','on-break') on UPDATE keeps concurrent break/end-break races resolving as 409 instead of overwriting. Topbar widget polls /api/me/clock-state every 30s; ticks the elapsed display once a second only while an event is open (saves cycles when nobody's clocked in, mirrors RunningTimerChip pattern from 2.3). |
| 2026-05-02 | 2.5.2 — Time Off Requests | migration 073 (time_off_requests with partial-status indexes for the approved-overlay + pending-queue hot paths; reuses shifts_set_updated_at trigger from 072; RLS reads any org member, writes self-or-admin), types/index.ts (TimeOffRequest + TimeOffStatus + TimeOffType), lib/timeoff/queries.ts (isTechOnTimeOff, getApprovedTimeOffOverlay, getApprovedTimeOffForTechs), /api/time-off-requests + [id] (cancel/edit) + [id]/decide (atomic eq('status','pending') flip stamps decided_by/decided_at), /(app)/time-off/{page, time-off-view (3 tabs: My Requests / Team Requests / Calendar overlay), time-off-form, time-off-approval-list}.tsx, AppLayout.tsx (Time Off entry next to Scheduler in both ownerNavItems + mechanic settingsFull list with CalendarOff icon), middleware.ts + tenant-routing.ts (time-off segment), shift-calendar.tsx (gray PTO bars overlaid above shift bars per-day) | Three of four acceptance clauses fully met. ✅ Tech submits → POST /api/time-off-requests → row pending. ✅ Admin approves → POST [id]/decide with eq('status','pending') guard → row flips, decided_by/decided_at stamped. ✅ Approved 3-day request → ShiftCalendar's ptoByDay map expands the multi-day range into per-day cells → gray "🌴 Tech · Type" bars render above shift bars. ⚠️ WO assignee picker conflict warning — same gap as 2.5.1: helpers ready (isTechOnTimeOff + getApprovedTimeOffForTechs), but no WO assignee-picker UI exists on the post-Operations-Hub WO detail (workflow-board uses assigned_mechanic_id only as a filter). Cross-wire becomes follow-up. | Path B: spec wrote types in DataStore.tsx + localStorage; ours live in apps/web/types/index.ts + Supabase. Decision goes through dedicated /decide endpoint (not generic PATCH) so the status-flip path is explicit + can carry manager_comment, and the eq('status','pending') filter on UPDATE is the atomic guard against double-decide races. notify_user_ids is UUID[] column for v0; cross-wire to sprint 0d sendNotification on decision is logged follow-up. RLS: requester can self-cancel pending only; admin can do anything. Default status='pending' (skip 'draft' for v0 — submit goes straight to pending). PTO bar in ShiftCalendar uses bg-slate-200/70 to read as "blocked" without competing visually with the colored shift status bars; renders BEFORE shifts in the day cell so the gray block visually pre-empts. ptoByDay expansion is O(days × pto-rows), bounded by the 4-month fetch window. |
| 2026-05-02 | 2.5.1 — Mechanic Scheduler (shifts) | migration 072 (shifts + shift_covers tables; partial-UNIQUE on (original_shift_id) WHERE status IN ('open','claimed') for one-live-cover-per-shift; RLS reads any org member, writes owner/admin), types/index.ts (Shift + ShiftStatus + ShiftChecklistItem + ShiftCover + ShiftCoverStatus; 'shifts' added to SavedViewModule union), lib/scheduler/queries.ts (getShiftsForTechnician, getActiveTechniciansAt, getTechShiftStatusMap, requestShiftCover, claimShiftCover), /api/shifts + [id] + [id]/request-cover, /api/shift-covers + [id] (open→claimed→approved/rejected state machine, approve flips Shift.technician_id + status='swapped'), /(app)/scheduler/{page, scheduler-view, shift-form, shift-calendar, my-shifts-view, shift-covers-list}.tsx, components/scheduler/todays-shifts-widget.tsx (embeddable Dashboard tile, drop-in ready), AppLayout.tsx (Scheduler entry next to Time clock in both ownerNavItems + mechanic settingsFull list with CalendarDays icon), middleware.ts + tenant-routing.ts (scheduler segment), lib/views/configs.ts (shifts MODULE_CONFIG with calendar default + 5 seeded views) | Two clauses of acceptance fully met, two partial. ✅ Manager creates shift: owner/admin opens /scheduler → "+ New shift" visible because isAdmin → ShiftForm POSTs /api/shifts → API validates org+role+tech-membership+end>start → row inserted. ✅ Sees on calendar: reload() fetches prev-month+next-3-months window → ShiftCalendar groups by day key, colors by status, shows tech name + time range. ⚠️ "Recurring" — single-shift create only this sprint; recurrence schema (RRULE / weekly repeat) is a logged follow-up. ⚠️ WO assignee picker cross-wire: getActiveTechniciansAt() + getTechShiftStatusMap() helpers ship today, but no WO assignee-picker UI exists on this branch (workflow-board uses assigned_mechanic_id only as a filter, not editor). Cross-wire becomes a follow-up — picker UI mounts, calls the helper, decorates each tech as on-shift / scheduled-later / off-shift. | Path B: shifts is the PLAN, time_entries (sprint 2.3) is the ACTUAL — coexists, neither replaces the other. RLS pattern mirrors time_entries: any org member READs (the team needs to see the schedule), only owner/admin WRITEs (techs don't self-schedule — swap requests go through shift_covers). Shift.reminders + Shift.checklist stored as JSONB to keep the cross-wire to sprint 0d's reminder_schedules flexible (eager vs lazy enqueue is a follow-up). One-live-cover-per-shift enforced by partial UNIQUE; claim race resolved by `eq('status','open')` filter on the UPDATE so two simultaneous claims = one wins, one gets zero-rows-affected. Approve transition does cover-flip + shift-reassign sequentially with best-effort rollback (Supabase JS doesn't expose Pg transactions; future: a stored proc). TodaysShiftsWidget is built but not yet mounted on Dashboard.tsx (703-line legacy) — same pattern Sprint 1.1/1.2 used for AircraftDetail.tsx. shifts MODULE_CONFIG seeds 5 views including calendar (default), board-by-status, board-by-technician — leverages 2.4 multi-view system though /scheduler page renders its own dedicated calendar instead of the generic ModuleViewShell (shifts need richer per-day rendering than the generic CalendarView). |
| 2026-05-01 | 2.4 — Multi-view system per module | migration 071 (saved_views with per-user + org-shared via NULL user_id), types/index.ts (SavedView + SavedViewModule + SavedViewType), lib/views/configs.ts (MODULE_CONFIGS registry + applySavedView + groupRows), lib/views/use-saved-views.ts (useSavedViews hook with sessionStorage active-view persistence + saveAs/updateActive/deleteActive), /api/saved-views + [id] (with default-view-clear + scope guard), components/views/{view-selector,view-settings,list-view,table-view,calendar-view,board-view,module-view-shell}.tsx, components/inspections/inspections-view.tsx (rewired from status-filter chips + flat list to ModuleViewShell — example wire-in for spec acceptance) | 12 files parse-clean (full tsc OOM-killed under session memory pressure; baseline = 19 errors / zero new from 2.3; 2.4 mostly adds isolated new files + a small surgical edit to inspections-view.tsx). Acceptance traced end-to-end on /inspections: ViewSelector shows 5 seeded views (All, Active, Calendar, Board by status, Table) + persisted views; ViewTypeSwitcher segmented control swaps list/calendar/table/board live; "Save as new view" dialog captures name + filters + sort + groupBy + view_type → POST /api/saved-views; sessionStorage persists active-view-id across navigations. | Generic shell intentionally decoupled from module specifics: each module declares fields + groupable + dateField + primaryField + seedViews via MODULE_CONFIGS in lib/views/configs.ts. Adding a new module = new entry in MODULE_CONFIGS + drop <ModuleViewShell module="..."> into the page. Filter editor in ViewSettings is enum/status-only for v0 (the field-type info is enough to add free-text + numeric range later). Inspections wired this sprint; logged follow-ups for the other 9 modules (work-orders, invoices, logbook, parts, POs, vendors, compliance, continued, approvals). user_id NULL = org-shared (admin+ to create/edit); user_id NOT NULL = personal. Default-view-clear logic runs in same scope (per-user OR org-shared) — flipping is_default doesn't affect the other scope's default. Drag-to-reorder and drag-between-columns on board are deferred (read-only board for v0). |
| 2026-05-01 | 2.3 — Live Time Clock on Work Orders | migration 070 (time_entries with partial UNIQUE on open per technician), types/index.ts (TimeEntry + TimeEntryWorkType), lib/timeclock/clock.ts (clockIn/clockOut/getOpenEntryForUser/aggregateWorkOrderHours), /api/time-entries + [id] + [id]/stop, /api/me/time-clock, /api/work-orders/[id]/labor, components/timeclock/{use-time-clock,running-timer-chip,time-clock-panel,time-clock-overview}.tsx, /(app)/time-clock/page.tsx, /(app)/work-orders/[id]/time-clock/page.tsx, components/shared/topbar.tsx (mounted RunningTimerChip), AppLayout.tsx + tenant-routing.ts + middleware.ts (time-clock segment) | 12 new files parse-clean (full tsc --noEmit OOM-killed under session memory pressure; baseline = 19 errors / zero new from 2.2; 2.3 only adds isolated new files with no existing-type changes, so baseline holds). Acceptance traced end-to-end: tech opens /work-orders/[id]/time-clock → fills rate + work_type → POST /api/time-entries → row inserted with start_time=now, end_time=null → RunningTimerChip on Topbar polls /api/me/time-clock and shows mm:ss live → Clock Out (or chip X) → POST /[id]/stop sets end_time → GET /api/work-orders/[id]/labor sums closed + open-running + manual line hours into total_hours / total_cost. | Partial UNIQUE index on (technician_id) WHERE end_time IS NULL is the DB safety net for "one open entry per tech"; clockIn() helper checks first to give a friendly 409 instead of letting the constraint trip. hourly_rate is FROZEN at clock-in (a later rate change doesn't retro-rewrite cost). RLS lets any org member READ entries (so the team's "currently working" board works) but writes are gated to self (technicians) or owner/admin (anyone in org). Coexists with work_order_lines.hours/rate (016) for manual labor lines — aggregate sums BOTH. RunningTimerChip ticks once a second when an open entry exists, no tick otherwise (saves cycles). Per-WO embed in legacy WorkOrderDetail is deferred; mounted at /work-orders/[id]/time-clock sub-route mirroring 1.1/1.2/1.3/1.4/1.5/2.1/2.2 pattern. |
| 2026-05-01 | 2.2 — Vendor Management | migration 069 (vendors table + vendor_id ALTERs on inventory_parts/purchase_orders/work_order_lines), types/index.ts (Vendor + VendorType), /api/vendors + [id] (with usage counts), components/vendors/{vendor-form,vendors-view}.tsx, /(app)/vendors/page.tsx, AppLayout.tsx + tenant-routing.ts + middleware.ts (vendors segment) | tsc --noEmit error count unchanged (19 → 19; zero new errors from 2.2). Acceptance traced end-to-end: list view shows vendors with type tints + approved pill + usage counts (parts/POs/WO lines); inline create + edit + archive + approve toggle; FK columns added to the three back-referencing tables; PO-create + part-create flows can now persist vendor_id alongside legacy `vendor TEXT` (UI-side migration of those forms is logged follow-up). | Per "add, don't replace": existing free-text `vendor TEXT` columns kept on inventory_parts/purchase_orders/work_order_lines; new NULLABLE `vendor_id` FKs added alongside (ON DELETE SET NULL preserves history). UI prefers vendor_id when present, falls back to legacy text. Approved-vendor flag is display-only in 2.2; future WO-line policy can refuse outside_service rows pointing to non-approved vendors. Archive over hard-delete is recommended (FKs SET NULL, but archiving keeps the row visible to historical UIs). UNIQUE(organization_id, name) — different orgs can each have their own "Aircraft Spruce" entry. List view does 3 cheap aggregate count queries to surface usage per vendor without a join — fine for v0 fleet sizes. |
| 2026-05-01 | 2.1 — Parts Inventory + Purchase Orders | migration 068 (3 tables + work_order_lines.inventory_part_id ALTER), types/index.ts (InventoryPart + PurchaseOrder + PurchaseOrderLine + PartClass + PurchaseOrderStatus), lib/inventory/{consume,po-numbers}.ts, lib/ai/orchestrator.ts (low-stock-card rule), /api/inventory-parts + [id] + [id]/{consume,restock}, /api/purchase-orders + [id] + [id]/fulfill, components/parts/{part-form,parts-inventory-view,po-form,pos-view,po-detail}.tsx, /(app)/parts/page.tsx (replaced redirect stub), /(app)/purchase-orders/{page,[id]/page}.tsx, AppLayout.tsx + tenant-routing.ts + middleware.ts (purchase-orders segment) | tsc --noEmit error count unchanged (19 → 19; zero new errors from 2.1). Acceptance traced end-to-end: (1) consumeInventoryPart() decrements qty_on_hand + emits low-stock signal on threshold-cross; consume endpoint exposes it for any caller; WO-line auto-call is logged follow-up. (2) /api/purchase-orders/[id]/fulfill calls restockInventoryPart per line, recomputes parent status (fulfilled / partially-fulfilled), stamps fulfilled_date when all lines received. (3) low-stock signal fires orchestrator low-stock-card rule (high priority compliance/maintenance ActionCard) → 0d cross-wire dispatches notifications to alert_emails recipients. UI: red-border row + Low pill + "Low stock only" filter chip + warning toast on threshold flip. | Local org inventory_parts is DISTINCT from external Atlas catalog (021 parts_searches/part_offers); coexists per "add, don't replace". work_order_lines.inventory_part_id is the bridge — NULLABLE so legacy free-text part_number rows still work. PO line FK is RESTRICT (can't delete a part referenced by POs); inventory_parts archive flag handles "remove from view but keep history". consumeInventoryPart() clamps at 0 + returns shortfall rather than throwing — UI surfaces the soft warning so operators can override. PO numbering is per-org-per-year sequence ("PO-2026-0001"); UNIQUE(org, po_number) is the safety net for the rare allocation race. /parts replaced the legacy redirect stub; legacy /maintenance?tab=parts (PartsSection localStorage UI) continues to work as a separate surface. /api/parts/search (Atlas catalog) is the existing external-search route — NOT folded in; spec note about returning local inventory first is logged as a follow-up. |
| 2026-05-01 | 1.5 — Customer Approvals portal | migration 067, types/index.ts (ApprovalRequest + ApprovalLineItem + ApprovalRequestStatus + ApprovalLineResponse), lib/approvals/token.ts, /api/approval-requests + [id] + [id]/send, /api/public/approvals/[token] + [token]/respond (PUBLIC, service-role), components/approvals/{approval-form,approvals-view,approval-detail,customer-approval-view}.tsx, /(app)/approvals/{page,[id]/page}.tsx, /approve/[token]/page.tsx (PUBLIC, outside (app) group), AppLayout.tsx + tenant-routing.ts + middleware.ts (approvals + approve segments) | tsc --noEmit error count unchanged (19 → 19; zero new errors from 1.5). Acceptance traced end-to-end: operator creates draft → POST /send → public_url returned → customer opens /approve/[token] (no auth, service-role validates token) → CustomerApprovalView shows org/aircraft/customer + line items → click Approve/Deny/Defer → POST /api/public/approvals/[token]/respond updates customer_response, recomputes parent status (partially-responded → completed), fires 0d notification to operator org. Cross-wire 1.5 → 1.4: deferred line items auto-create continued_items rows with aircraft FK + idempotency via approval_line_items.resulting_continued_item. | Public token = 32-char Crockford base32 (~160 bits entropy, no I/L/O/U for clarity), generated server-side via crypto.randomBytes; UNIQUE indexed on approval_requests.public_token. Public route lives OUTSIDE (app)/ route group (no auth required); APIs use createServiceSupabase() to bypass RLS while validating token shape + status. Public GET refuses drafts (404) and expired (410) — the operator hasn't actually shared, or the link is dead. Existing estimates table (040) is internal-facing pricing; approval_requests is the customer-facing public-token portal — different shape, different purpose. /respond is the SINGLE source of truth: it updates the line, optionally creates a continued_item (idempotent via FK), recomputes parent status, fires notification. Operator-side ApprovalDetail offers a "Copy link" + "Preview" affordance so they can paste the URL into any channel until SendGrid integration lands. |
| 2026-05-01 | 1.4 — Continued Items (deferred maintenance) | migration 066, types/index.ts (ContinuedItem + ContinuedItemStatus + ContinuedItemPriority), /api/continued-items + [id] + [id]/resolve, /api/aircraft/[id]/continued, components/continued/{continued-item-form,continued-items-list,aircraft-continued-panel,continued-page-view}.tsx, /(app)/continued/page.tsx, /(app)/aircraft/[id]/continued/page.tsx, AppLayout.tsx + tenant-routing.ts + middleware.ts (continued segment) | tsc --noEmit error count unchanged (19 → 19; zero new errors from 1.4). Acceptance traced end-to-end: create item → row stored with aircraft FK → visible at /continued (org-wide) and /aircraft/[id]/continued (per-aircraft) → resolve via inline form (status='completed', resolved_on_work_order set, resolved_at stamped) → item moves to Resolved tab. ON DELETE SET NULL on both discovered_on_work_order and resolved_on_work_order means deleting old WOs preserves the deferred-item history. | Items follow the AIRCRAFT, not the WO: aircraft_id ON DELETE CASCADE; both work_order FKs are ON DELETE SET NULL. Pilots can't write — they squawk through the squawk pipeline (013/026); mechanic+ triage continued items. PATCH /api/continued-items/[id] auto-stamps resolved_at/resolved_by when status flips to 'completed'/'wont-fix'; the dedicated POST /resolve endpoint also accepts work_order_id and notes. POST /resolve appends resolution notes (with date prefix) to existing notes rather than replacing — preserves history. Spec asks for embed in AircraftDetail.tsx + WO-creation pull-in checklist + WorkOrderPanel "Mark as Continued" button — all logged as follow-ups; DB FKs and APIs are ready. |
| 2026-05-01 | 1.3 — Inspections + Procedures / Checklists | migration 065 (5 tables), types/index.ts (Procedure + ProcedureSection + ProcedureItem + Inspection + InspectionResult + unions), lib/inspections/status.ts (computeInspectionProgress + deriveInspectionStatus + isAnswered), /api/procedures + [id], /api/inspections + [id] + [id]/results + [id]/complete, components/inspections/{procedure-builder,procedures-view,inspections-view,procedure-runner}.tsx, /(app)/procedures/{page,[id]/page}.tsx, /(app)/inspections/{page,[id]/page}.tsx, AppLayout.tsx + tenant-routing.ts + middleware.ts (procedures + inspections) | tsc --noEmit error count unchanged (19 → 19; zero new errors from 1.3). Acceptance traced end-to-end: create procedure (multi-section + multi-input-type items) → start inspection from procedure (snapshots procedure_name) → ProcedureRunner per-item save (PUT upserts to inspection_results, first save flips status draft→in-progress) → Complete inspection (POST /complete derives status server-side via shared status helper; refuses pending items unless ?force=1, then flips to complete-requires-attention). UI auto-disables runner once status is complete*. | procedure_sections + procedure_items are *child tables* (not JSONB) so inspection_results can FK to a specific item — renaming an item or reordering doesn't orphan history. inspection_results.value stored as JSONB to handle string|boolean|number without a discriminator column (frontend reads input_type to interpret). procedure_name_snapshot column on inspections preserves audit context if the procedure is renamed/archived. Wholesale-replace shape on PATCH /procedures/[id] (delete-and-reinsert sections+items) — granular section/item edit endpoints are a logged follow-up. work_order_checklist_items (038) is a *different* per-WO concept; left untouched per "add, don't replace". Photo / signature uploads stub the input ("Mark recorded" button) until file storage wiring lands. |
| 2026-05-01 | 1.2 — Compliance / Maintenance Tracking | migration 064, types/index.ts (ComplianceItem + unions), lib/compliance/{compute,recompute}.ts, /api/compliance-items + [id] + [id]/complete, /api/aircraft/[id]/compliance, components/compliance/{compliance-item-form,compliance-due-list,aircraft-compliance-panel,compliance-page-view}.tsx, /(app)/compliance/page.tsx, /(app)/aircraft/[id]/compliance/page.tsx, lib/ai/orchestrator.ts (added compliance-due-card rule), /api/meter-readings/route.ts (cross-wire to recomputeCompliance), AppLayout.tsx + tenant-routing.ts + middleware.ts (compliance segment) | tsc --noEmit error count unchanged (19 baseline → 19; zero new errors from 1.2). Acceptance traced end-to-end: create item with cal+hour intervals → recomputeCompliance fills next_due_* + status → log meter reading → meter-readings POST cross-wire fires recomputeCompliance → if status flips to overdue/due-soon, emitSignal('compliance-due') → orchestrator's compliance-due-card rule produces urgent/high ActionCard with markComplianceComplete suggested action → 0d cross-wire dispatches notifications. Mark complete: POST /complete updates last_completed_*, re-runs recompute, status returns to current. | Whichever-comes-first compute lives in lib/compliance/compute.ts as a *pure* function (no DB I/O) so it's easily testable. recompute.ts handles I/O + emits compliance-due signals only on status FLIPS (not on every recompute) to avoid notification spam. Heuristic: aircraft.current_hours = latest reading on a meter named Hobbs/Tach (case-insensitive substring match); current_cycles from a meter named Cycles. Aviation-specific reminders table (013) and aircraft_ad_applicability (014) untouched — compliance_items coexists per "add, don't replace". AircraftDetail.tsx (3,626 lines legacy) untouched; AircraftCompliancePanel mounted at /aircraft/[id]/compliance sub-route mirroring Sprint 1.1 pattern. |
| 2026-05-01 | 1.1 — Meter Profiles & Aircraft Times | migration 063, types/index.ts (MeterProfile, MeterDefinition, MeterReading, MeterUnit; aircraft.meter_profile_id), lib/meters/current.ts, /api/meter-profiles + [id], /api/meter-readings + [id], /api/aircraft/[id]/meters (GET + PATCH), components/meters/{meter-profile-form,meter-profiles-view,meter-reading-form,aircraft-meter-panel}.tsx, /(app)/meters/page.tsx, /(app)/aircraft/[id]/meters/page.tsx, AppLayout.tsx (Meters nav for both personas), tenant-routing.ts + middleware.ts (meters segment) | tsc --noEmit error count unchanged (19 baseline → 19; zero new errors from 1.1). Acceptance traced end-to-end: create profile → assign to aircraft → log reading → emitSignal('meter-reading') fires (closes Sprint 0c follow-up; orchestrator's existing rule produces an ActionCard) → AircraftMeterPanel refreshes → current values + history populate → edit historical reading via inline editor → list re-renders. | Path B: meter_definitions stored as a child table (rather than JSONB on meter_profiles) so meter_readings can FK to a specific meter row → clean cascades + meter rename doesn't break history. Existing aircraft.total_time_hours kept for back-compat; meter_readings is now the source of truth. AircraftDetail.tsx (3,626 lines legacy redesign) NOT modified — embeddable AircraftMeterPanel mounted at /aircraft/[id]/meters sub-route instead. Pilot role can write readings (not edit profiles). Cross-wire to Sprint 0c: meter-reading POST emits the AISignal that the orchestrator's compliance-glance rule consumes — meter-reading acceptance criterion from 0c is now wired with real data. |
| 2026-05-01 | 0d — Notification system | migration 062, lib/notifications/{types,dispatch,reminders,use-notifications}.ts, /api/notifications/{route,[id],mark-all-read,preferences,dispatch,reminders,reminders/tick}, components/notifications/{notification-bell,notification-preferences}.tsx, /(app)/settings/notifications/page.tsx, components/shared/topbar.tsx (mounted NotificationBell), lib/ai/orchestrator.ts (cross-wired urgent/high cards → sendNotification) | tsc --noEmit error count unchanged (19 baseline → 19; zero new errors from 0d). Reminder pipeline traced end-to-end: scheduleReminders() → 1 row per offset → tickReminders selects due rows → sendNotification fans out per-recipient × per-channel honoring notification_preferences (with category-default fallback) → in-app delivers, email/push/SMS marked skipped+TODO until adapters wired. Bell hooks polling + read sync; preferences UI flips per-cell. | Persisted notifications + per-category/per-channel preferences instead of spec's in-memory pipeline (multi-device read sync requires server state). Email/Push/SMS adapters explicit TODO with delivery_status='skipped' (not 'failed') so the in-app row doesn't lie. Existing aviation `reminders` table (013) untouched — generic `reminder_schedules` (062) is the unified delivery layer per "add, don't replace". Cross-wire from Sprint 0c: urgent/high ActionCards auto-fire notifications via the unified dispatcher. |
| 2026-05-01 | 0c — AI Orchestration foundation | migration 061, lib/ai/{types,signals,tool-registry,prompts,orchestrator,use-ai-inbox}.ts, /api/ai/{signals/emit,inbox,inbox/[id],orchestrator/tick,tools/[name]}, components/ai/{action-card,ai-inbox}.tsx, /(app)/inbox/page.tsx, AppLayout.tsx (Inbox nav for both personas), tenant-routing.ts + middleware.ts (allow inbox/locations/org segments) | tsc --noEmit error count unchanged (19 baseline → 19; zero new errors from 0c). Acceptance pipeline traced end-to-end in code: signal emit → orchestrator tick → rule applies → card upserted → /api/ai/inbox returns it → ActionCard renders w/ suggested-action button → POST /api/ai/tools/[name] dispatches via registry. 12 of 13 spec tools registered with TODO handlers per spec ("Backend required. Mark TODO clearly."). Real WO-creation tool wiring deferred to Feature 1.x / 2.x sprints. | Path B: persisted ai_signals + ai_action_cards tables instead of in-memory event bus, so orchestrator survives serverless restarts. Lazy-tick on every /api/ai/inbox GET replaces the spec's "every minute cron" until a vercel.json schedule entry is added. |
| 2026-05-01 | 0b — Persona system | migration 060, lib/persona/{config,server,use-persona}.ts, /api/me/persona, /api/me/orgs (extended), AppContext.tsx (widened Persona, hydrate from server, persist via API), ask-experience.tsx (narrow to AskPersona), workspace-client.tsx (narrow ArtifactEmptyState) | tsc --noEmit error count unchanged (19 baseline → 19; zero new errors from 0b). Acceptance traced end-to-end: org-switch (0a hard reload) re-fetches /api/me/orgs → AppContext re-hydrates active_persona → PERSONA_CONFIG drives sidebar/AI/home. Shop sidebar variant deferred to Phase 5 (config slot reserved). | Persona scoped *per membership* (DB column on organization_memberships), not just user-global; user_profiles.persona stays as fallback. setPersona is now optimistic + auto-persists via /api/me/persona. |
| 2026-05-01 | 0a — Multi-Org / Multi-Location data model | migration 059, types/index.ts, lib/org/{context,use-org}.ts, /api/me/{orgs,active-org,active-location}, /api/locations + [id], /(app)/locations + /(app)/org/switch, AppLayout.tsx | tsc --noEmit clean (exit 0). Manual two-org switch verified via /org/switch + /api/me/active-org cookie. Per-list location filter UI deferred to follow-up. | Path B adaptation: Supabase columns instead of localStorage; App Router routes instead of routes.tsx; existing CRUD shape preserved |
```

## 8. Open decisions / blockers

_List anything you're waiting on Andy for. Anything that came up during a sprint that needs human judgment._

```
- [ ] (example) Need Airbly API key — Andy to create developer account
- [ ] (example) Need Stripe account for Phase 6.3
- [ ] (0a follow-up) Filter-by-location dropdown on Aircraft / Work Orders / Invoices / Documents lists — `active_location_id` cookie + /api/me/active-location route exist; consumers not yet wired
- [ ] (0a follow-up) Backfill `location_id` on existing aircraft / work_orders / invoices for tenants that already have data — currently NULL on migration; UI tolerates NULL
- [ ] (0a follow-up) Org-create + invite flow — /org/switch only lets you pick from existing memberships; creating a new org / accepting an invite still goes through pre-existing flows in /onboarding and /settings
- [ ] (0b follow-up) Shop persona sidebar variant — PERSONA_CONFIG.shop is reserved + the DB column accepts 'shop', but AppLayout only renders owner|mechanic nav today. Build the dispatcher/foreman sidebar in Phase 5 alongside Smart Home Screen
- [ ] (0b follow-up) Ask/Chat AI prompt sourcing — /api/ask + /api/chat still take persona from request body and use hardcoded prompts. Switch them to PERSONA_CONFIG[persona].aiSystemPrompt sourced from getCurrentPersona() so the persona system is the single source of truth
- [ ] (0b follow-up) Org-switch redirect should honor PERSONA_CONFIG[persona].homeRoute — currently always lands on /dashboard which is suboptimal for mechanic persona (home is /mechanic)
- [ ] (0b follow-up) AppLayout switchPersona signature is still typed `'owner' | 'mechanic'` — widen to Persona once shop sidebar exists
- [ ] (0c follow-up) Wire emitSignal('meter-reading') from the actual meter-reading endpoint (lands with Feature 1.1 Meter Profiles)
- [ ] (0c follow-up) Wire emitSignal('wo-closed' / 'doc-uploaded') from existing /api/work-orders close + /api/upload/complete routes
- [ ] (0c follow-up) Add vercel.json schedule entry calling /api/ai/orchestrator/tick every minute (per spec) — currently lazy-ticked on inbox fetch only
- [ ] (0c follow-up) Implement the 12 TODO tool handlers (createWorkOrder, addMeterReading, etc.) — staged across Phase 1.x / 2.x sprints
- [ ] (0c follow-up) Step 3 of orchestrator loop: every 10 minutes, run LLM pass over recent signals → emit insight ActionCards (Phase 5.1 / 5.2)
- [ ] (0c follow-up) Step 4 of orchestrator loop: every hour, run ML predictions → anomaly cards (Phase 5.3 Predictive Maintenance ML)
- [ ] (0c follow-up) AI Inbox should support Supabase realtime subscription on ai_action_cards instead of 60s polling
- [ ] (0d follow-up) Wire SendGrid in lib/notifications/dispatch.ts:deliverEmail() — currently stubbed with delivery_status='skipped'
- [ ] (0d follow-up) Wire Web Push (VAPID + service worker) in deliverPush() — service worker registration + push_subscriptions table
- [ ] (0d follow-up) Wire Twilio in deliverSms() — Pro-tier feature gate per spec
- [ ] (0d follow-up) vercel.json schedule entry calling /api/notifications/reminders/tick every minute (or every 5 min — these aren't latency-critical)
- [ ] (0d follow-up) Have existing aviation `reminders` table (013) enqueue generic reminder_schedules so users get the same delivery experience
- [ ] (0d follow-up) NotificationBell should subscribe to Supabase realtime on `notifications WHERE user_id = auth.uid()` instead of 60s polling
- [ ] (0d follow-up) /settings page should link to /settings/notifications (currently only the bell-dropdown footer links to it)
- [x] (0c follow-up RESOLVED in 1.1) emitSignal('meter-reading') wired from /api/meter-readings POST — orchestrator's existing rule now produces ActionCards from real data instead of synthetic harness signals.
- [ ] (1.1 follow-up) Embed AircraftMeterPanel as a tab inside the legacy AircraftDetail.tsx (3,626 lines redesign component) — currently mounted at /aircraft/[id]/meters sub-route to keep the diff tight
- [ ] (1.1 follow-up) Add/remove/reorder meter definitions on an existing profile — the create route seeds initial meters, but a profile-edit UI is needed for evolving fleets
- [ ] (1.1 follow-up) Logbook entry creation should auto-fill totalTime/hobbs/tach via getCurrentMeterReading — types/index.ts LogbookEntry already has the columns; the wiring in /api/logbook-entries POST is the missing piece
- [ ] (1.1 follow-up) Seed every new org with template meter profiles (Piston Single, Twin Piston, Turbine, Helicopter) so users land on /meters with sensible defaults
- [ ] (1.1 follow-up) /api/aircraft/[id]/meters PATCH duplicates a tiny chunk of aircraft-edit logic — when the legacy aircraft-edit surface is rewritten, fold meter_profile_id into the unified PATCH body
- [ ] (1.2 follow-up) Embed AircraftCompliancePanel as a tab inside legacy AircraftDetail.tsx
- [ ] (1.2 follow-up) Add a "Compliance — Due Soon" widget to Dashboard.tsx (703-line legacy redesign component) — currently the global Due List is reachable via the dedicated /compliance page only
- [ ] (1.2 follow-up) Wire markComplianceComplete tool handler in lib/ai/tool-registry.ts — currently a stub. Once wired, the SuggestedAction button on compliance-due ActionCards will execute the same flow as POST /api/compliance-items/[id]/complete
- [ ] (1.2 follow-up) When a Work Order closes, prompt to mark linked compliance items complete (auto-recompute next-due) — spec calls for this; needs the WO-close hook from a future Phase 2 sprint
- [ ] (1.2 follow-up) Per-meter-profile "primary meter" config — current heuristic in recompute.ts assumes a meter named Hobbs/Tach for hours and Cycles for cycles; users with non-standard meter names need an explicit pointer
- [ ] (1.2 follow-up) Aviation reminders table (013) + aircraft_ad_applicability (014) should optionally enqueue compliance_items — converges the multiple sources of truth over time
- [ ] (1.2 follow-up) Edit-mode of compliance items via the form (not just inline status / mark-complete) — currently you delete + recreate to rename or change intervals
- [ ] (1.3 follow-up) WorkOrder UI ("Attach Procedure" button) — spec calls for it but legacy WO surface is in MaintenancePage / WorkOrderDetail (multi-thousand-line components). The DB FK (inspections.linked_work_order) and API support are already there; just need a UI button on the WO surface
- [ ] (1.3 follow-up) Photo + signature uploads in ProcedureRunner — DB column inspection_results.photo_urls is ready; need Supabase Storage wiring + signature pad
- [ ] (1.3 follow-up) Granular section/item edit endpoints — currently PATCH /api/procedures/[id] does wholesale delete-and-reinsert if `sections` is passed
- [ ] (1.3 follow-up) Cross-wire 1.3 → 1.2: when an Inspection completes with linked_compliance_items, auto-mark those compliance items complete via /api/compliance-items/[id]/complete
- [ ] (1.3 follow-up) Cross-wire 1.3 → 0c: emit `inspection-complete` AISignal on /complete (would need adding to AISignalType union); orchestrator can produce ActionCards summarizing the completed inspection
- [ ] (1.3 follow-up) AircraftDetail.tsx tab embed for inspections — sub-route /aircraft/[id]/inspections is the natural addition (mirrors 1.1 + 1.2 pattern); not yet built
- [ ] (1.3 follow-up) Wire `createInspection` AI tool handler in lib/ai/tool-registry.ts — currently a stub from 0c; can now wrap the new /api/inspections POST
- [ ] (1.4 follow-up) Embed AircraftContinuedItemsPanel as a tab inside legacy AircraftDetail.tsx
- [ ] (1.4 follow-up) WorkOrderPanel "Mark as Continued" button → POST /api/continued-items with discovered_on_work_order set. DB + API ready
- [ ] (1.4 follow-up) WO-creation pull-in checklist: when creating a new WO for an aircraft, show open continued items so the user can add them to the WO. On WO close, auto-resolve the pulled-in items via POST /api/continued-items/[id]/resolve
- [ ] (1.4 follow-up) Bulk-create continued items from a failed inspection (cross-wire 1.3 → 1.4): on POST /api/inspections/[id]/complete with failed_items > 0, prompt the user to convert each failed item into a continued_item
- [ ] (1.4 follow-up) AI Inbox surface for high/urgent open continued items > N days old (cross-wire 1.4 → 0c). Add `continued-stale` to AISignalType + a rule that fires when an open urgent item's age exceeds threshold
- [ ] (1.5 follow-up) WorkOrderPanel "Send for Customer Approval" button — DB has approval_line_items.work_order_line_id ready; needs UI on legacy WO surface
- [ ] (1.5 follow-up) Wire SendGrid email delivery in /api/approval-requests/[id]/send — currently returns the public_url for the operator to paste manually. Same SendGrid integration that's pending for Sprint 0d email notifications
- [ ] (1.5 follow-up) WO billing validation: approval_line_items where customer_response='denied' should block billing on linked work_order_line_id. Needs WO close hook
- [ ] (1.5 follow-up) Wire `sendApprovalRequest` AI tool handler in lib/ai/tool-registry.ts — currently a stub from 0c; can now wrap POST /api/approval-requests
- [ ] (1.5 follow-up) Photo upload for line items — DB column approval_line_items.photo_urls TEXT[] is ready; needs Supabase Storage wiring (same pending follow-up as Sprint 1.3 inspection_results.photo_urls)
- [ ] (1.5 follow-up) Cross-wire 1.5 → 0c: emit `approval-response` AISignal on customer responses (already in AISignalType union from 0c) — orchestrator can produce ActionCards summarizing customer decisions
- [ ] (1.5 follow-up) Realtime updates on operator-side ApprovalDetail — currently requires manual refresh. Supabase realtime subscription on approval_line_items would surface customer responses live
- [ ] (1.5 follow-up) Operator can edit line items on a draft request — currently the create form takes the items, but no PATCH path for individual lines (must DELETE + recreate the request)
- [ ] (2.1 follow-up) Auto-decrement on WO line install — consumeInventoryPart() is ready; need the legacy WO line POST route to call it when a line with inventory_part_id is added (or when WO status transitions to "In Progress")
- [ ] (2.1 follow-up) Wire searchParts AI tool handler in lib/ai/tool-registry.ts to return local inventory_parts FIRST before falling back to Atlas catalog (021) — spec note in Feature 2.1 §Integration
- [ ] (2.1 follow-up) Wire addPartToWorkOrder AI tool handler — currently a 0c stub; can now wrap consumeInventoryPart + the WO line API once the auto-decrement above lands
- [ ] (2.1 follow-up) Weighted-average unit_cost on restock — currently latest-cost heuristic via unit_cost_override. WAC = Σ(qty * cost) / total_qty, recomputed per restock
- [ ] (2.1 follow-up) Email delivery of low-stock alerts to alert_emails recipients — Sprint 0d's deliverEmail() is currently stubbed pending SendGrid; the low-stock signal payload already carries alert_emails so the cross-wire fires correctly when SendGrid lands
- [ ] (2.1 follow-up) Per-line fulfillment receipts (idempotency keys) — currently repeated /fulfill calls with the same body over-increment. UI tracks outstanding qty so this is operationally safe but not idempotent
- [ ] (2.1 follow-up) Bulk import inventory parts (CSV) — most shops have an existing parts master in Excel; one-shot import would unblock real usage
- [ ] (2.2 follow-up) Migrate inventory_parts.vendor TEXT and purchase_orders.vendor TEXT into vendor_id FKs on the existing UI flows (PartForm, PurchaseOrderForm). DB FK is ready; forms still write the legacy TEXT column
- [ ] (2.2 follow-up) work_order_lines.vendor_id is wired in DB; legacy WO line UI still writes `vendor TEXT`. Plumb the vendor picker into the outside_service line type once the legacy WO surface is rewritten
- [ ] (2.2 follow-up) Approved-vendor enforcement — currently display-only. Future policy: refuse outside_service WO line creation when vendor.approved=false (or warn + soft-allow with override)
- [ ] (2.2 follow-up) Vendor detail page (/vendors/[id]) showing all parts/POs/WO lines that reference this vendor — usage counts already computed in /api/vendors/[id]
- [ ] (2.2 follow-up) De-dup helper: when a vendor name matches an existing free-text vendor on parts/POs/WO lines, offer to bulk-link those rows to the new vendor record on create
- [ ] (2.3 follow-up) Embed TimeClockPanel as a tab inside legacy WorkOrderDetail (3,626-line redesign component). DB + API + component are ready; tab-embed mirrors the same plumbing pattern as Sprints 1.1/1.2/1.3/1.4
- [ ] (2.3 follow-up) "Forgot to clock out" anomaly signal — emit `anomaly` AISignal (already in AISignalType union from 0c) when an open time_entry exceeds N hours (e.g. 12). Orchestrator rule produces an ActionCard prompting the technician to close the entry
- [ ] (2.3 follow-up) Per-line clock-in: spec mentions optional work_order_line_id on TimeEntry. Schema + API support it; UI currently clocks at the WO level. Future: pick a labor line in the clock-in form so cost rolls into that specific line
- [ ] (2.3 follow-up) Cron auto-close: 24h after start, force-close open entries (end_time = start + threshold) and emit a notification. Prevents accidental forever-running entries from billing customers for hours nobody actually worked
- [ ] (2.3 follow-up) Wire searchParts AI tool handler — currently a 0c stub. Now that inventory + time-clock exist, the AI tool registry handlers can be implemented one at a time
- [ ] (2.3 follow-up) Roll time_entries totals INTO the WO's `total_labor` column (currently only computed via aggregateWorkOrderHours). The WO row's denormalized total is used by some legacy invoice paths
- [ ] (2.4 follow-up) Wire ModuleViewShell into the remaining 9 modules: work-orders (legacy redesign — separate sprint), invoices, logbook, parts (parts-inventory-view), purchase-orders (pos-view), vendors (vendors-view), compliance (compliance-page-view), continued (continued-page-view), approvals (approvals-view). Each = add a MODULE_CONFIGS entry + drop in <ModuleViewShell module="…"> with a renderListRow
- [ ] (2.4 follow-up) Filter editor: free-text + numeric range widgets (currently enum/status checkboxes only — field types support all but the UI only renders enums)
- [ ] (2.4 follow-up) Drag-to-reorder + drag-between-columns on board view — currently read-only
- [ ] (2.4 follow-up) Column visibility + reordering on table view via display_config (schema is ready; UI is hard-coded to defaultVisible fields)
- [ ] (2.4 follow-up) Day-detail modal on calendar — clicking a day with N events should pop a list rather than just visible-3 + "+N" indicator
- [ ] (2.4 follow-up) Saved-view duplication ("Save a copy of this view") — currently the "Save as new" dialog seeds from the active view, but a one-click duplicate-and-rename would be friendlier
- [ ] (2.4 follow-up) Free-form `display_config` migration: define the per-module schema (which fields visible, calendar date override, etc.) and let ViewSettings edit it
- [ ] (2.4 follow-up) Org-shared views currently must be created via API with `scope=org` — UI doesn't expose the scope toggle yet (admin+ only)
- [ ] (2.6.1 follow-up) Tool reminder/notification — when a tool hits `tolerance_days` window before due, fire a sprint 0d notification to the org's mechanic role recipients. Hook into the existing `scheduleReminders()` flow.
- [ ] (2.6.1 follow-up) Use-count-based intervals — `calibration_interval_uses` field is in the schema but `assertToolCalibrated()` only checks date-based intervals. Need a separate `tool_uses` counter table (or aggregate work_order_tool_uses by tool_id) + `next_calibration_at_use_count` field.
- [ ] (2.6.1 follow-up) Dashboard widget — "Tools — Calibration Due Soon" tile listing the next 5 tools due in 30 days. Pattern matches the deferred TodaysShiftsWidget mount on the legacy 703-line Dashboard.tsx.
- [ ] (2.6.1 follow-up) ModuleViewShell wire-in for /tools — register a 'tools' MODULE_CONFIG entry so the saved-views machinery applies (Board by category, Calendar by next_calibration_date, Table view).
- [ ] (2.6.1 follow-up) Calibration certificate file upload — `certificate_urls` and `certificate_url` fields are in the schema; need Supabase Storage wiring + drag-drop in the calibration log form. Same pending storage wiring pattern as sprint 1.3 inspection_results.photo_urls.
- [ ] (2.6.1 follow-up) Bulk import tools (CSV) — most shops have a tool register in Excel; one-shot import would unblock real adoption.
- [ ] (2.6.2 follow-up) Mount DocumentExpirationPanel on Dashboard.tsx (703-line legacy redesign) — same pattern as deferred TodaysShiftsWidget mount. Component is built at components/documents/expiration-panel.tsx with self-contained fetch.
- [ ] (2.6.2 follow-up) AircraftDetail.tsx tab embed for documents (currently mounted at /aircraft/[id]/documents sub-route) — mirrors 1.1 / 1.2 / 1.3 / 1.4 pattern.
- [ ] (2.6.2 follow-up) ModuleViewShell wire-in for /documents/expiring — register a 'documents-expiring' MODULE_CONFIG so saved-views machinery applies (board by status, calendar by expiration_date, table by category × persona).
- [ ] (2.6.2 follow-up) File upload pairing — currently the form is metadata-only ("track expiration"). When the user uploads a renewal PDF later via /api/upload, the existing doc row should ATTACH to it (FK file_path) rather than creating a duplicate. Needs a "link to expiring record" affordance in upload-dropzone.
- [ ] (2.6.2 follow-up) Auto-create renewal task on expiration — when a doc flips to expired and renewal_tracking_id is NULL, auto-spawn a continued_item (1.4) or work_order so the renewal isn't lost. Schema column is already there; needs the cross-wire.
- [ ] (2.6.2 follow-up) Notification preferences UI for category 'document-expiration' — sprint 0d's notification_preferences table accepts the category; the preferences page just needs an entry in its category list.
- [ ] (2.6.2 follow-up) Email + push channels in reminder_offsets — server accepts channels[] but the form only writes ['in-app']. Add a channel-picker checkbox group to ExpiringDocForm once email delivery (SendGrid) is wired in sprint 0d.
- [ ] (2.6.2 follow-up) Bulk-import expiring docs (CSV) — onboarding flow for an org with 30 aircraft + 30 registrations + 10 mechanics × 4 certs each. CSV import would unblock real adoption.
- [ ] (4.3 follow-up) Spec 4.4 inference engine — multi-source priority pick (airbly > fsp > adsb-exchange > flightaware) + cross-source dedupe (±10 min start_time same aircraft → keep highest priority, mark "verified by N sources" on the kept row via supersedes/superseded_by FKs already in 077). Lands when 4.1 + 4.2 are unblocked.
- [ ] (4.3 follow-up) FlightAware AeroAPI client — fall through when ADSB Exchange returns no data for a tail. Mirror lib/telemetry/sources/adsb-exchange.ts shape; add 'flightaware' branch in syncOneAircraftByOrg that runs only when adsb-exchange is empty for the day.
- [ ] (4.3 follow-up) Phase 5 ActionCard rule for `flight-detected` AISignal — "We detected a 1.4-hour flight today (estimated). Confirm or edit Hobbs?" with toolCall pointing at /api/flight-events/[id]/confirm. The signal is emitted; no rule consumes it yet.
- [ ] (4.3 follow-up) Cycles per flight beyond 1 — current detector treats one continuous airborne window as 1 cycle. Touch-and-go training and ag operations need to count cycles inside a single airborne window (altitude dipping below threshold = additional cycle). Flight detector signature already accepts ping[].alt; add a cycle counter that walks the path.
- [ ] (4.3 follow-up) Settings UI: per-aircraft "telemetry_sources" management (enable/disable + priority + tach_buffer + tach_to_hobbs_ratio overrides). v0 reads/writes via API only; the row is auto-provisioned but there's no UI to edit it. Lands with Spec 4.4's settings exposure.
- [ ] (4.3 follow-up) Initial-baseline UX — ADSB inference only writes meter_readings when a baseline already exists for that meter. First-time owners with no manual reading get flight_events but no meter_readings; needs an explicit "Set initial Hobbs/Tach" prompt on the Sync tab when aircraft.meter_profile_id is set but no readings exist yet.
- [ ] (4.3 follow-up) Cron tick observability — current /api/integrations/adsb/sync returns the per-aircraft results array but doesn't persist it. Future ingestion_failures-style table for telemetry_sync_logs would let admins see "last 24h of ADSB sync attempts per aircraft" instead of hunting Vercel logs.
- [ ] (5.1 follow-up) LLM-driven card generation — Spec 5.1 implies richer cards generated via Anthropic Claude (e.g. "We detected a 1.4-hour flight today (estimated). Confirm or edit Hobbs?" with an evidence chain explaining the inference). v0 ships rule-based scans only; LLM layer reads recent ai_signals + entity context, composes via structured output, writes through the same ai_action_cards upsert path. Server-side only — never expose ANTHROPIC_API_KEY to the client.
- [ ] (5.1 follow-up) Move generateProactiveCards from per-render call to a cron tick — currently runs on every SmartHome render (cheap but wasteful when the same owner reloads 50 times/day). Vercel cron at every 5 min mirroring the 4.3 ADSB sync cadence + a manual trigger via /api/ai/cards/regenerate would keep cards fresh without page-load latency.
- [ ] (5.1 follow-up) Card resolution → DB write back — when a SuggestedAction toolCall succeeds (e.g. "Mark compliance complete"), the card resolves but the underlying compliance_items row should also flip to current. The existing /api/ai/tools/[name] dispatcher is a stub; wire real handlers per tool name.
- [ ] (5.1 follow-up) Dashboard widget retirement plan — both /dashboard (legacy stat cards) and /my-aircraft (SmartHome) render now. Phase 5.x should retire /dashboard entirely or fold its specific widgets (TodaysShiftsWidget, DocumentExpirationPanel, etc.) into the SmartHome layout per persona.
- [ ] (5.1 follow-up) Aircraft card "today's airborne hours" sums only same-day flight_events with start_time >= midnight — but a flight that starts 23:55 and ends 00:30 next day shows on the wrong card. Use UTC-local conversion to bucket by aircraft's home_base timezone instead of UTC midnight.
- [ ] (5.1 follow-up) Mechanic SmartHome currently filters action cards into two stacks (queue + heads-up) by category. Could be smarter: group by aircraft (since a mechanic working on N12345 wants all N12345 cards together), or by due-by-time-of-day. Spec 5.2 will likely fold this in.
- [ ] (5.1 follow-up) Greeting copy is purely deterministic (time-of-day + persona-shaped sentence). LLM hookup in Spec 5.4 (Voice & Camera) brings personalization — "Hey Andy, Cessna 172 is ready. You haven't flown in 12 days; weather looks good for tomorrow." That's the upgrade target.
- [ ] (5.6 follow-up) Operator-side preview of ai_explanation_md before sending — currently the operator hits "Send" and the explanation generates in the background. A "Preview customer view" page would let the operator regenerate, hand-edit, or override per-line explanations before the customer ever sees them. Edits would set source='manual' on the line and skip future auto-regens.
- [ ] (5.6 follow-up) Streaming response for the Ask box — currently the answer arrives in one shot after the LLM finishes (~3-5s for 2-3 sentences). Anthropic's streaming endpoint + Vercel ReadableStream would make the UX feel instant. Keep the rate-limit + cap logic untouched.
- [ ] (5.6 follow-up) Multi-language support — explainer + ask system prompts default to English. A real shop in Quebec or Sao Paulo wants the customer view in fr-CA / pt-BR. Trivial to add as a second arg on the prompts; the English-only default just leaves the door open.
- [ ] (5.6 follow-up) Re-trigger on description edit — if an operator edits an approval_line_item.description AFTER explanations have generated, the ai_explanation_md becomes stale. PATCH /api/approval-line-items/[id] should null out ai_explanation_generated_at so the next send/regen call re-explains. Trivial trigger or app-layer flush.
- [ ] (5.6 follow-up) Cache identical line items across approvals — "Replace #3 cylinder $1,400" shows up on many WOs. A content-hash index on (description, hours, parts) could let us reuse a prior ai_explanation_md across customers, cutting cost ~50%+ at fleet scale. Privacy-safe because explanations don't include customer PII per the SYSTEM_PROMPT constraint.
- [ ] (5.6 follow-up) Admin dashboard for ai_activity_log — token spend per scope, failure rate, cost-cap hits. Org owners + admins should see "this month: $4.20 across 168 calls" without hunting Supabase. /(app)/admin/ai-spend page; the table + RLS are already in place.
- [ ] (5.6 follow-up) Wire Anthropic Tool Use for stricter structured output — currently explainApprovalLine returns free-form Markdown; the SYSTEM_PROMPT keeps it well-shaped but doesn't enforce schema. Tool-use guarantees a structured response and lets us add JSON fields like {risk_if_deferred, est_turnaround_days} that the UI surfaces separately.
- [ ] (5.5 follow-up) Mount AuditFindingsCard somewhere — built but not yet rendered on any surface. Natural homes: a "Recent audits" tab on a future shop-foreman dashboard, OR an embedded findings panel inside /work-orders/[id] detail page. Phase 5.2's filter work may fold this into the main AI Inbox (category filter) instead.
- [ ] (5.5 follow-up) Inspections module cross-wire — Spec 5.5 says "When a WO closes OR an Inspection completes, the AI Inspector reviews". Currently only WO-close fires the audit; /api/inspections/[id]/complete from sprint 1.3 should also fire an inspection-specific audit (different rule set: every checklist item answered, no RII gaps, photo evidence on items requiring it).
- [ ] (5.5 follow-up) Persist audit history — currently auditWorkOrder upserts a single dedupe-keyed card per WO; a separate ai_audit_runs table would let admins see "this WO was audited 3 times, here's the diff over time". Useful for shop foremen tracking whether techs are improving.
- [ ] (5.5 follow-up) Configurable rule thresholds per org — labor variance threshold (currently 20%) should be tunable; some shops run intentionally lean and consider 30% normal. Future ai_inspector_settings table keyed by (org, rule_id) with per-rule overrides.
- [ ] (5.5 follow-up) Suggested actions beyond "Open work order" — for missing-rii findings, a one-click "Send for second signoff" to a specific A&P. For missing-photos, a "Request photo" cross-wire to the assignee. Today every audit card just links back to the WO.
- [ ] (5.5 follow-up) Inventory consumption ledger — current "inventory-mismatch" check is a heuristic on orphaned part_ids + negative qty_on_hand. A real inventory_consumptions audit table would let us trace exactly which line consumed which part for which WO and surface mismatches like "billed 5 oil filters but only 3 decrements were recorded".
- [ ] (2.5.3 follow-up) Auto-close stale open ClockEvents — cron after 12h or 24h: if status≠'clocked-out', clock-out automatically + emit anomaly AISignal so the tech sees it next session. Pairs with sprint 2.3's "forgot to clock out" follow-up; the same cron can sweep both tables.
- [ ] (2.5.3 follow-up) Hours-by-Employee-by-Week report — sum total_hours across clock_events grouped by ISO week + employee. UI: /reports/timetracking. Spec mentions parity with Coast's Timetracking Report.
- [ ] (2.5.3 follow-up) Optional shift_id auto-link on clock-in — if the tech has a sprint 2.5.1 Shift active right now (start_time ≤ now < end_time), auto-set clock_events.shift_id. Currently shift_id is hand-passable but we don't infer it.
- [ ] (2.5.3 follow-up) Photo check-in (image_url field is ready) — wire Supabase Storage upload from the topbar widget for orgs that want it. UI defers until a "Require check-in photo" org policy lands.
- [ ] (2.5.3 follow-up) Pre-clock-in PTO guard — if isTechOnTimeOff(empId, today) returns true, refuse the clock-in with a friendly "you're scheduled off today, contact your manager." Cross-wires sprint 2.5.2's helper.
- [ ] (2.5.2 follow-up) WO assignee picker PTO conflict warning — `isTechOnTimeOff()` + `getApprovedTimeOffForTechs()` ship in lib/timeoff/queries.ts but no actual picker UI exists on the post-Operations-Hub WO detail (same gap as 2.5.1's getActiveTechniciansAt). Build the picker, call both helpers, decorate each tech as on-shift / on-PTO / off-shift / scheduled-later.
- [ ] (2.5.2 follow-up) Sprint 0d sendNotification cross-wire on decision — when a request flips to approved/denied, fan out to notify_user_ids + the requester via the unified dispatcher. Notification category 'time-off' to add to the preferences UI.
- [ ] (2.5.2 follow-up) Pre-decision conflict check — when manager hits Approve, check if the requested window contains scheduled shifts for that tech (sprint 2.5.1 shifts table). Surface "Tech has 2 shifts in this window — they'll need cover" warning before confirming.
- [ ] (2.5.2 follow-up) Year-rollup PTO balance — track per-employee accruals (vacation days remaining) by category. Schema not in this sprint; needs a `time_off_balances` table + accrual rules per org.
- [ ] (2.5.2 follow-up) Cancel-after-approval — currently approved requests can't be cancelled by the requester. Real-world: tech recovers from illness early. Add an admin-only "Withdraw approval" path or let the tech re-submit a "shorten" request.
- [ ] (2.5.2 follow-up) ModuleViewShell wire-in for /time-off — register a 'time-off' MODULE_CONFIG so saved-views machinery applies (board-by-status, calendar overlay as a real saved view, etc.).
- [ ] (2.5.1 follow-up) Recurring shift schedule — sprint 2.5.1 ships single-shift create only. Need to extend shifts schema (RRULE column + parent_shift_id self-FK, OR a `recurrence_rules` child table) and the form (pick recurrence: daily / weekly-by-weekday / monthly + count or end_date) so a manager can create "Mike: Mon-Fri 8a-4p, every week through end of quarter" in one go. Spec acceptance text says "recurring morning shift" — current single-create works but doesn't match that wording.
- [ ] (2.5.1 follow-up) WO assignee picker cross-wire — `getActiveTechniciansAt()` + `getTechShiftStatusMap()` ship in lib/scheduler/queries.ts but no actual picker UI exists on the post-Operations-Hub WO detail (workflow-board uses assigned_mechanic_id only as a filter). Build the picker into work-order-detail-client.tsx (or a new components/work-orders/assignee-picker.tsx), call the helper, decorate each tech as on-shift / scheduled-later-today / off-shift.
- [ ] (2.5.1 follow-up) Mount TodaysShiftsWidget on Dashboard.tsx — component is built at components/scheduler/todays-shifts-widget.tsx with self-contained fetch; needs surgical insertion into the 703-line legacy Dashboard layout at the right grid slot. Pattern mirrors the AircraftCompliancePanel mount deferral from Sprint 1.1/1.2.
- [ ] (2.5.1 follow-up) Cross-wire to sprint 0d reminder_schedules — Shift.reminders is currently free-form JSONB. Decide whether to eagerly enqueue rows in reminder_schedules at shift-create time (so the existing tick processor delivers them) or lazily resolve at tick-time. Plus: build a structured reminder editor in ShiftForm to replace the JSON pass-through.
- [ ] (2.5.1 follow-up) Notify cross-wire on shift_cover transitions — open→claimed should ping the requester; claimed→approved should ping both parties + the shift's original technician_id. Sprint 0d's sendNotification dispatcher is the right hook; tracked alongside the still-stubbed SendGrid email channel.
- [ ] (2.5.1 follow-up) "Forgot to clock out" + "missed shift" anomaly signals — emit `anomaly` AISignal (already in 0c union) when a shift's end_time has passed and status is still 'scheduled' / 'in-progress'. Pairs naturally with sprint 2.3's open-time-entry stale-check follow-up.
- [ ] (2.5.1 follow-up) Approve transition is two writes (cover row + shift row) with best-effort rollback — convert to a Postgres stored proc (`approve_shift_cover(cover_id)`) so the swap is atomic at the DB layer, not at the app layer.
- [ ] (2.5.1 follow-up) ModuleViewShell wire-in for /scheduler — currently renders a dedicated ShiftCalendar per Spec 2.5.1 ("default view: ShiftCalendar — Month or Week"); the shifts MODULE_CONFIG is registered but not consumed by the page yet. Future: build a ShiftBoardView + reuse generic ListView/TableView so saved-view machinery applies.
- [ ] (2.5.1 follow-up) shift_covers approve also sets the original Shift's `technician_id` to the covering tech — but does NOT preserve a record of who originally had the shift. Add `original_technician_id` column to shifts (or denormalize from shift_covers history) so audits can trace "this shift was originally Mike's, swapped to Andy on Tuesday."
- [ ] (deploy-prep 2026-05-01) Migration drift detected during deploy prep — three issues, resolution deferred until after Operations Hub merge to main and after current sprint pipeline is unblocked:
    1. **14 migrations applied directly to staging without committing matching `.sql` files.** Names listed in the audit transcript: `lead_spine`, `add_created_by_to_thread_messages`, `clear_document_derived_artifacts_function_timeout`, `enforce_platform_admin_single_user`, `ingestion_failures_log`, `claude_review_requests`, `heal_attempt_count_and_app_settings`, `documents_needs_human_review`, `backfill_document_date_from_title`, `parts_searches_rename_query_to_search_query`, `seed_universal_ads_and_dummy_applicability`, `logbook_entries_signature_audit`, `work_orders_ai_summary`, `locations_and_multi_org`. The repo is non-reproducible from `supabase db reset` against a fresh DB until these are backfilled into `supabase/migrations/`.
    2. **Main branch references 4 migration files (`065_inspections`, `068_inventory_parts`, `070_time_entries`, `071_saved_views`) that have not been applied to staging** — this means main cannot smoke-test on staging until they are applied or until staging is reset. Sprint 2.x is partially applied: `locations` + `vendors` exist on staging, but `inspections` / `inventory_parts` / `time_entries` / `saved_views` do not.
    3. **File-name collision:** `claude/gallant-mendeleev-8d5357` has `059_clear_document_derived_artifacts_rpc.sql`, `main` has `059_locations_and_multi_org.sql`. Both have already been applied to staging (under different `name` keys in `supabase_migrations.schema_migrations`), so staging is fine — but one of the files must be renumbered before the branches can merge cleanly.
```

## 9. File map (what each sprint added)

_Append a table after each sprint listing the files created/modified. Helps future sessions navigate without re-reading the whole repo._

```
| Sprint | New files | Modified files |
|--------|-----------|----------------|
| 0a — Multi-Org / Multi-Location | supabase/migrations/059_locations_and_multi_org.sql · apps/web/lib/org/context.ts · apps/web/lib/org/use-org.ts · apps/web/app/api/me/orgs/route.ts · apps/web/app/api/me/active-org/route.ts · apps/web/app/api/me/active-location/route.ts · apps/web/app/api/locations/route.ts · apps/web/app/api/locations/[id]/route.ts · apps/web/app/(app)/locations/page.tsx · apps/web/app/(app)/locations/locations-view.tsx · apps/web/app/(app)/org/switch/page.tsx · apps/web/app/(app)/org/switch/org-switch-view.tsx | apps/web/types/index.ts · apps/web/components/redesign/AppLayout.tsx · apps/web/lib/auth/tenant-routing.ts (already fixed in Phase 1 debug) · apps/web/middleware.ts (already fixed in Phase 1 debug) |
| 0b — Persona system | supabase/migrations/060_membership_persona.sql · apps/web/lib/persona/config.ts · apps/web/lib/persona/server.ts · apps/web/lib/persona/use-persona.ts · apps/web/app/api/me/persona/route.ts | apps/web/app/api/me/orgs/route.ts (embeds active_persona) · apps/web/components/redesign/AppContext.tsx (widened Persona type, server hydration, auto-persist) · apps/web/components/ask/ask-experience.tsx (narrowed to AskPersona) · apps/web/app/(app)/workspace/workspace-client.tsx (narrowed ArtifactEmptyState persona) |
| 0c — AI Orchestration foundation | supabase/migrations/061_ai_orchestration.sql · apps/web/lib/ai/types.ts · apps/web/lib/ai/signals.ts · apps/web/lib/ai/tool-registry.ts · apps/web/lib/ai/prompts.ts · apps/web/lib/ai/orchestrator.ts · apps/web/lib/ai/use-ai-inbox.ts · apps/web/app/api/ai/signals/emit/route.ts · apps/web/app/api/ai/inbox/route.ts · apps/web/app/api/ai/inbox/[id]/route.ts · apps/web/app/api/ai/orchestrator/tick/route.ts · apps/web/app/api/ai/tools/[name]/route.ts · apps/web/components/ai/action-card.tsx · apps/web/components/ai/ai-inbox.tsx · apps/web/app/(app)/inbox/page.tsx | apps/web/components/redesign/AppLayout.tsx (Inbox nav entry for both personas) · apps/web/lib/auth/tenant-routing.ts (added inbox/locations/org reserved segments) · apps/web/middleware.ts (added inbox/locations/org to appRoutes) |
| 0d — Notification system | supabase/migrations/062_notifications.sql · apps/web/lib/notifications/types.ts · apps/web/lib/notifications/dispatch.ts · apps/web/lib/notifications/reminders.ts · apps/web/lib/notifications/use-notifications.ts · apps/web/app/api/notifications/route.ts · apps/web/app/api/notifications/[id]/route.ts · apps/web/app/api/notifications/mark-all-read/route.ts · apps/web/app/api/notifications/preferences/route.ts · apps/web/app/api/notifications/dispatch/route.ts · apps/web/app/api/notifications/reminders/route.ts · apps/web/app/api/notifications/reminders/tick/route.ts · apps/web/components/notifications/notification-bell.tsx · apps/web/components/notifications/notification-preferences.tsx · apps/web/app/(app)/settings/notifications/page.tsx | apps/web/components/shared/topbar.tsx (NotificationBell mounted) · apps/web/lib/ai/orchestrator.ts (urgent/high cards → sendNotification cross-wire) |
| 1.1 — Meter Profiles & Aircraft Times | supabase/migrations/063_meters.sql · apps/web/lib/meters/current.ts · apps/web/app/api/meter-profiles/route.ts · apps/web/app/api/meter-profiles/[id]/route.ts · apps/web/app/api/meter-readings/route.ts · apps/web/app/api/meter-readings/[id]/route.ts · apps/web/app/api/aircraft/[id]/meters/route.ts · apps/web/components/meters/meter-profile-form.tsx · apps/web/components/meters/meter-profiles-view.tsx · apps/web/components/meters/meter-reading-form.tsx · apps/web/components/meters/aircraft-meter-panel.tsx · apps/web/app/(app)/meters/page.tsx · apps/web/app/(app)/aircraft/[id]/meters/page.tsx | apps/web/types/index.ts (Aircraft.meter_profile_id + MeterProfile/MeterDefinition/MeterReading/MeterUnit/MeterReadingSource types) · apps/web/components/redesign/AppLayout.tsx (Meters nav for both personas) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'meters' to reserved + appRoutes) |
| 1.2 — Compliance / Maintenance Tracking | supabase/migrations/064_compliance_items.sql · apps/web/lib/compliance/compute.ts · apps/web/lib/compliance/recompute.ts · apps/web/app/api/compliance-items/route.ts · apps/web/app/api/compliance-items/[id]/route.ts · apps/web/app/api/compliance-items/[id]/complete/route.ts · apps/web/app/api/aircraft/[id]/compliance/route.ts · apps/web/components/compliance/compliance-item-form.tsx · apps/web/components/compliance/compliance-due-list.tsx · apps/web/components/compliance/aircraft-compliance-panel.tsx · apps/web/components/compliance/compliance-page-view.tsx · apps/web/app/(app)/compliance/page.tsx · apps/web/app/(app)/aircraft/[id]/compliance/page.tsx | apps/web/types/index.ts (ComplianceItem + ComplianceItemType + ComplianceSource + ComplianceStatus) · apps/web/lib/ai/orchestrator.ts (added compliance-due-card rule) · apps/web/app/api/meter-readings/route.ts (cross-wired recomputeCompliance) · apps/web/components/redesign/AppLayout.tsx (Compliance nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'compliance' to reserved + appRoutes) |
| 1.3 — Inspections + Procedures / Checklists | supabase/migrations/065_inspections.sql · apps/web/lib/inspections/status.ts · apps/web/app/api/procedures/route.ts · apps/web/app/api/procedures/[id]/route.ts · apps/web/app/api/inspections/route.ts · apps/web/app/api/inspections/[id]/route.ts · apps/web/app/api/inspections/[id]/results/route.ts · apps/web/app/api/inspections/[id]/complete/route.ts · apps/web/components/inspections/procedure-builder.tsx · apps/web/components/inspections/procedures-view.tsx · apps/web/components/inspections/inspections-view.tsx · apps/web/components/inspections/procedure-runner.tsx · apps/web/app/(app)/procedures/page.tsx · apps/web/app/(app)/procedures/[id]/page.tsx · apps/web/app/(app)/procedures/[id]/edit-client.tsx · apps/web/app/(app)/inspections/page.tsx · apps/web/app/(app)/inspections/[id]/page.tsx | apps/web/types/index.ts (Procedure + ProcedureSection + ProcedureItem + Inspection + InspectionResult + ProcedureItemInputType + InspectionStatus) · apps/web/components/redesign/AppLayout.tsx (Inspections nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'procedures' + 'inspections' to reserved + appRoutes) |
| 1.4 — Continued Items (deferred maintenance) | supabase/migrations/066_continued_items.sql · apps/web/app/api/continued-items/route.ts · apps/web/app/api/continued-items/[id]/route.ts · apps/web/app/api/continued-items/[id]/resolve/route.ts · apps/web/app/api/aircraft/[id]/continued/route.ts · apps/web/components/continued/continued-item-form.tsx · apps/web/components/continued/continued-items-list.tsx · apps/web/components/continued/aircraft-continued-panel.tsx · apps/web/components/continued/continued-page-view.tsx · apps/web/app/(app)/continued/page.tsx · apps/web/app/(app)/aircraft/[id]/continued/page.tsx | apps/web/types/index.ts (ContinuedItem + ContinuedItemStatus + ContinuedItemPriority) · apps/web/components/redesign/AppLayout.tsx (Continued nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'continued' to reserved + appRoutes) |
| 1.5 — Customer Approvals portal | supabase/migrations/067_customer_approvals.sql · apps/web/lib/approvals/token.ts · apps/web/app/api/approval-requests/route.ts · apps/web/app/api/approval-requests/[id]/route.ts · apps/web/app/api/approval-requests/[id]/send/route.ts · apps/web/app/api/public/approvals/[token]/route.ts · apps/web/app/api/public/approvals/[token]/respond/route.ts · apps/web/components/approvals/approval-form.tsx · apps/web/components/approvals/approvals-view.tsx · apps/web/components/approvals/approval-detail.tsx · apps/web/components/approvals/customer-approval-view.tsx · apps/web/app/(app)/approvals/page.tsx · apps/web/app/(app)/approvals/[id]/page.tsx · apps/web/app/approve/[token]/page.tsx | apps/web/types/index.ts (ApprovalRequest + ApprovalLineItem + ApprovalRequestStatus + ApprovalLineResponse) · apps/web/components/redesign/AppLayout.tsx (Approvals nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'approve' + 'approvals' to reserved; 'approvals' to tenant-prefixes + appRoutes; 'approve' stays out of tenant scope so the public route resolves) |
| 2.1 — Parts Inventory + Purchase Orders | supabase/migrations/068_inventory_parts.sql · apps/web/lib/inventory/consume.ts · apps/web/lib/inventory/po-numbers.ts · apps/web/app/api/inventory-parts/route.ts · apps/web/app/api/inventory-parts/[id]/route.ts · apps/web/app/api/inventory-parts/[id]/consume/route.ts · apps/web/app/api/inventory-parts/[id]/restock/route.ts · apps/web/app/api/purchase-orders/route.ts · apps/web/app/api/purchase-orders/[id]/route.ts · apps/web/app/api/purchase-orders/[id]/fulfill/route.ts · apps/web/components/parts/part-form.tsx · apps/web/components/parts/parts-inventory-view.tsx · apps/web/components/parts/po-form.tsx · apps/web/components/parts/pos-view.tsx · apps/web/components/parts/po-detail.tsx · apps/web/app/(app)/purchase-orders/page.tsx · apps/web/app/(app)/purchase-orders/[id]/page.tsx | apps/web/types/index.ts (InventoryPart + PurchaseOrder + PurchaseOrderLine + PartClass + PurchaseOrderStatus) · apps/web/lib/ai/orchestrator.ts (added low-stock-card rule) · apps/web/components/redesign/AppLayout.tsx (Parts + Purchase orders nav) · apps/web/app/(app)/parts/page.tsx (replaced redirect stub) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'purchase-orders' to reserved + appRoutes) |
| 2.2 — Vendor Management | supabase/migrations/069_vendors.sql · apps/web/app/api/vendors/route.ts · apps/web/app/api/vendors/[id]/route.ts · apps/web/components/vendors/vendor-form.tsx · apps/web/components/vendors/vendors-view.tsx · apps/web/app/(app)/vendors/page.tsx | apps/web/types/index.ts (Vendor + VendorType) · apps/web/components/redesign/AppLayout.tsx (Vendors nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'vendors' to reserved + tenant-prefixes + appRoutes) |
| 2.3 — Live Time Clock on Work Orders | supabase/migrations/070_time_entries.sql · apps/web/lib/timeclock/clock.ts · apps/web/app/api/time-entries/route.ts · apps/web/app/api/time-entries/[id]/route.ts · apps/web/app/api/time-entries/[id]/stop/route.ts · apps/web/app/api/me/time-clock/route.ts · apps/web/app/api/work-orders/[id]/labor/route.ts · apps/web/components/timeclock/use-time-clock.ts · apps/web/components/timeclock/running-timer-chip.tsx · apps/web/components/timeclock/time-clock-panel.tsx · apps/web/components/timeclock/time-clock-overview.tsx · apps/web/app/(app)/time-clock/page.tsx · apps/web/app/(app)/work-orders/[id]/time-clock/page.tsx | apps/web/types/index.ts (TimeEntry + TimeEntryWorkType) · apps/web/components/shared/topbar.tsx (mounted RunningTimerChip) · apps/web/components/redesign/AppLayout.tsx (Time clock nav) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'time-clock' to reserved + tenant-prefixes + appRoutes) |
| 2.4 — Multi-view system per module | supabase/migrations/071_saved_views.sql · apps/web/lib/views/configs.ts · apps/web/lib/views/use-saved-views.ts · apps/web/app/api/saved-views/route.ts · apps/web/app/api/saved-views/[id]/route.ts · apps/web/components/views/view-selector.tsx · apps/web/components/views/view-settings.tsx · apps/web/components/views/list-view.tsx · apps/web/components/views/table-view.tsx · apps/web/components/views/calendar-view.tsx · apps/web/components/views/board-view.tsx · apps/web/components/views/module-view-shell.tsx | apps/web/types/index.ts (SavedView + SavedViewModule + SavedViewType) · apps/web/components/inspections/inspections-view.tsx (rewired to use ModuleViewShell — example wire-in for the spec acceptance) |
| 2.6.1 — Tool Management & Calibration | supabase/migrations/075_tools_and_calibration.sql · apps/web/lib/tools/queries.ts · apps/web/app/api/tools/route.ts · apps/web/app/api/tools/[id]/route.ts · apps/web/app/api/tools/[id]/calibrations/route.ts · apps/web/app/api/tools/[id]/checkout/route.ts · apps/web/app/api/tool-checkouts/[id]/return/route.ts · apps/web/app/api/work-orders/[id]/tools/route.ts · apps/web/app/(app)/tools/page.tsx · apps/web/app/(app)/tools/tools-view.tsx · apps/web/app/(app)/tools/tool-form.tsx · apps/web/app/(app)/tools/[id]/page.tsx · apps/web/app/(app)/tools/[id]/tool-detail.tsx · apps/web/components/work-orders/wo-tools-panel.tsx | apps/web/types/index.ts (Tool + CalibrationEvent + ToolCheckout + WorkOrderToolUse + ToolCategory/ToolStatus/CalibrationResult/ToolReturnCondition unions) · apps/web/components/redesign/AppLayout.tsx (Tools entry next to Vendors with Wrench icon, both nav surfaces) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'tools' to reserved + tenant-prefixes + appRoutes) · apps/web/app/(app)/work-orders/[id]/work-order-detail-client.tsx (new Tools tab between AD/SB and Logbook, mounts WoToolsPanel — full WO server-side overdue-tool block) |
| 5.5 — AI Inspector (auto-audit closed work orders) | supabase/migrations/079_audit_findings_category.sql · apps/web/lib/ai/inspectors/wo-auditor.ts · apps/web/app/api/work-orders/[id]/audit/route.ts · apps/web/app/api/cron/wo-audit/route.ts · apps/web/components/inspections/audit-findings-card.tsx | apps/web/lib/ai/types.ts (ActionCardCategory adds 'audit-finding') · apps/web/components/ai/action-card.tsx (CATEGORY_ICON + CATEGORY_TINT for audit-finding) · apps/web/app/api/work-orders/[id]/route.ts (PATCH hooks auditWorkOrder background-fire on status close) · apps/web/vercel.json (added /api/cron/wo-audit at every-15-min) |
| 5.6 — Smart Customer Approvals (LLM-rewritten explanations + Ask box) | supabase/migrations/078_smart_approvals.sql · apps/web/lib/ai/anthropic.ts · apps/web/lib/ai/explainers/approval-line.ts · apps/web/app/api/approval-requests/[id]/regenerate-explanations/route.ts · apps/web/app/api/public/approvals/[token]/ask/route.ts | apps/web/types/index.ts (ApprovalLineItem extended with ai_explanation_md/_at/_model/_input_tokens/_output_tokens) · apps/web/components/approvals/customer-approval-view.tsx (Plain English ↔ Technical toggle pill + per-line conditional render + Ask a question box) · apps/web/app/api/approval-requests/[id]/send/route.ts (auto-trigger background regenerateForApprovalRequest after sending) · apps/web/app/api/public/approvals/[token]/route.ts (GET select extended with ai_explanation_md + ai_explanation_generated_at) · .env.local.example (ANTHROPIC_API_KEY documented as server-only) |
| 5.1 — Smart Home Screen (persona-aware action stack) | apps/web/lib/ai/cards/generators.ts · apps/web/components/home/SmartHome.tsx · apps/web/components/home/AIGreeting.tsx · apps/web/components/home/AircraftCard.tsx · apps/web/components/home/ActionCardStack.tsx · apps/web/app/(app)/my-day/page.tsx | apps/web/app/(app)/my-aircraft/page.tsx (rewritten — was legacy aircraft list, now mounts SmartHome) · apps/web/components/redesign/AppLayout.tsx (owner top nav: Dashboard → "Home" /my-aircraft; mechanic top nav: Dashboard → "My Day" /my-day) · apps/web/lib/persona/config.ts (PERSONA_CONFIG.owner.homeRoute /dashboard → /my-aircraft; PERSONA_CONFIG.mechanic.homeRoute /mechanic → /my-day) · apps/web/middleware.ts + apps/web/lib/auth/tenant-routing.ts (added /my-day to appRoutes + RESERVED_TOP_LEVEL_SEGMENTS + TENANT_SCOPED_ROUTE_PREFIXES) |
| 4.3 — ADSB Fallback (telemetry, public-data path) | supabase/migrations/077_telemetry_adsb.sql · apps/web/lib/telemetry/inference.ts · apps/web/lib/telemetry/flightDetector.ts · apps/web/lib/telemetry/sources/adsb-exchange.ts · apps/web/app/api/integrations/adsb/sync/route.ts · apps/web/app/api/aircraft/[id]/flights/route.ts · apps/web/app/api/flight-events/[id]/confirm/route.ts · apps/web/app/(app)/aircraft/[id]/sync/page.tsx · apps/web/components/telemetry/aircraft-sync-view.tsx | apps/web/types/index.ts (FlightEvent + TelemetrySourceConfig + TelemetryPing + TachInferenceResult + TelemetryProvider + TelemetryConfidenceTier; MeterReadingSource widened) · apps/web/vercel.json (added /api/integrations/adsb/sync to crons every 5 min) · .env.local.example (RAPIDAPI_ADSB_EXCHANGE_KEY documented as server-only) |
| 2.6.2 — Document Expiration & Reminders | supabase/migrations/076_documents_expiration.sql · apps/web/lib/documents/expiration.ts · apps/web/app/api/documents/expiring/route.ts · apps/web/app/api/documents/[id]/expiration/route.ts · apps/web/app/(app)/documents/expiring/page.tsx · apps/web/app/(app)/documents/expiring/expiring-view.tsx · apps/web/app/(app)/documents/expiring/expiring-doc-form.tsx · apps/web/app/(app)/aircraft/[id]/documents/page.tsx · apps/web/components/documents/expiration-panel.tsx | apps/web/types/index.ts (Document extended with target_persona/expiration_category/has_expiration/expiration_date/effective_date/reminder_offsets/expiration_status/issued_by/document_number/renewal_tracking_id; ExpirationPersona + ExpirationStatus + ReminderOffsetSpec; OWNER/MECHANIC/SHOP_DOC_CATEGORIES + DOC_CATEGORIES_BY_PERSONA seeded constants) · apps/web/components/redesign/AppLayout.tsx (CalendarClock "Expirations" entry between Compliance and Inspections in both ownerNavItems + mechanic settingsFull list — `/documents` already covered by tenant-routing + middleware allowlists) |
| 2.5.3 — Daily Clock In/Out | supabase/migrations/074_clock_events.sql · apps/web/lib/clock/queries.ts · apps/web/app/api/clock-events/route.ts · apps/web/app/api/clock-events/[id]/route.ts · apps/web/app/api/clock-events/[id]/break/route.ts · apps/web/app/api/clock-events/[id]/clock-out/route.ts · apps/web/app/api/me/clock-state/route.ts · apps/web/components/clock/clock-in-widget.tsx · apps/web/app/(app)/clock/page.tsx · apps/web/app/(app)/clock/clock-view.tsx | apps/web/types/index.ts (ClockEvent + BreakInterval + ClockEventStatus; TimeEntry.clock_event_id field) · apps/web/components/redesign/AppLayout.tsx (Clock In/Out entry next to Time Off, Clock icon) · apps/web/components/shared/topbar.tsx (mounts ClockInWidget alongside RunningTimerChip) · apps/web/lib/timeclock/clock.ts (sprint 2.3 clockIn() now stamps time_entries.clock_event_id from the active daily ClockEvent) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'clock' to reserved + tenant-prefixes + appRoutes) |
| 2.5.2 — Time Off Requests | supabase/migrations/073_time_off_requests.sql · apps/web/lib/timeoff/queries.ts · apps/web/app/api/time-off-requests/route.ts · apps/web/app/api/time-off-requests/[id]/route.ts · apps/web/app/api/time-off-requests/[id]/decide/route.ts · apps/web/app/(app)/time-off/page.tsx · apps/web/app/(app)/time-off/time-off-view.tsx · apps/web/app/(app)/time-off/time-off-form.tsx · apps/web/app/(app)/time-off/time-off-approval-list.tsx | apps/web/types/index.ts (TimeOffRequest + TimeOffStatus + TimeOffType) · apps/web/components/redesign/AppLayout.tsx (Time Off entry next to Scheduler in both ownerNavItems + mechanic settingsFull list, CalendarOff icon) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'time-off' to reserved + tenant-prefixes + appRoutes) · apps/web/app/(app)/scheduler/scheduler-view.tsx + shift-calendar.tsx (fetch + overlay approved PTO bars) |
| 2.5.1 — Mechanic Scheduler (shifts) | supabase/migrations/072_shifts_and_shift_covers.sql · apps/web/lib/scheduler/queries.ts · apps/web/app/api/shifts/route.ts · apps/web/app/api/shifts/[id]/route.ts · apps/web/app/api/shifts/[id]/request-cover/route.ts · apps/web/app/api/shift-covers/route.ts · apps/web/app/api/shift-covers/[id]/route.ts · apps/web/app/(app)/scheduler/page.tsx · apps/web/app/(app)/scheduler/scheduler-view.tsx · apps/web/app/(app)/scheduler/shift-form.tsx · apps/web/app/(app)/scheduler/shift-calendar.tsx · apps/web/app/(app)/scheduler/my-shifts-view.tsx · apps/web/app/(app)/scheduler/shift-covers-list.tsx · apps/web/components/scheduler/todays-shifts-widget.tsx | apps/web/types/index.ts (Shift + ShiftStatus + ShiftChecklistItem + ShiftCover + ShiftCoverStatus; 'shifts' added to SavedViewModule union) · apps/web/components/redesign/AppLayout.tsx (Scheduler entry next to Time clock in both ownerNavItems + mechanic settingsFull list, CalendarDays icon) · apps/web/lib/auth/tenant-routing.ts + apps/web/middleware.ts (added 'scheduler' to reserved + tenant-prefixes + appRoutes) · apps/web/lib/views/configs.ts (shifts MODULE_CONFIG with calendar default + 5 seeded views) |
```

## 10. Glossary (acronyms / terms specific to this project)

- **WO** = Work Order
- **PM** = Preventive Maintenance
- **OSR** = Outside Service Repair (vendor-performed work)
- **A&P** = Airframe & Powerplant (FAA mechanic certificate)
- **IA** = Inspection Authorization (senior A&P)
- **RII** = Required Inspection Item
- **AD** = Airworthiness Directive
- **SB** = Service Bulletin
- **Hobbs** = engine-running time meter
- **Tach** = engine RPM-based time meter (≈ 85% of Hobbs at cruise)
- **FSP** = FlightSchedule Pro
- **POH** = Pilot Operating Handbook
- **AFM** = Aircraft Flight Manual

## 11. Decisions made (append-only — never overwrite)

_Architecture decisions worth remembering. Each decision: date, what, why._

```
| Date | Decision | Why |
|------|----------|-----|
| 2026-05-01 | All data scoped by orgId from Phase 0 | Avoid migration later when multi-tenancy ships |
| 2026-05-01 | ADSB Exchange = primary fallback, FlightAware = backup | Cheaper at SaaS scale; FA is pay-per-query |
| 2026-05-01 | Tach inference: Hobbs = ADSB_time + 0.4 hr × cycles | Industry standard for piston aircraft taxi/runup buffer |
| 2026-05-01 | Persona drives UI via `PERSONA_CONFIG` map, not branching | Easier to maintain than per-component if/else |
| 2026-05-01 | **Path B adaptation:** apply spec on top of existing Next.js `apps/web/` codebase; do NOT spin up the spec's React Router + localStorage shape | Keeps RAG, Supabase, billing, ingestion infra; spec was written for a Vite single-page tree but the real product is a Next.js App Router monorepo |
| 2026-05-01 | Active org persisted via `active_organization_id` cookie set by `/api/me/active-org`; server reads cookie via `requireAppServerSession()` and re-renders RSC against the new org | Drop-in for `useOrg()` hook from spec; no client store needed; cookie survives reloads and isolates RSC fetches |
| 2026-05-01 | Active location persisted via `active_location_id` cookie set by `/api/me/active-location`; consumers opt-in (not auto-applied) | Lets pages choose to filter by location instead of forcing every list to add a `location_id = ?` filter on day one |
| 2026-05-01 | RLS policies on `locations` mirror existing org-membership pattern (read = any accepted member, write = mechanic+/admin/owner) | Consistent with the rest of the schema; `MECHANIC_AND_ABOVE` already enforced at the API layer |
| 2026-05-01 | One-level location hierarchy via `parent_location_id` self-FK with cycle guard at API layer (`parent_id !== id`) | Spec asked for "KAPA → Hangar 14 → Bay 3"; deeper trees are over-engineered for v0 |
| 2026-05-01 | **Persona scoped per-membership** (organization_memberships.persona) rather than user-global (user_profiles.persona) | Same person can be owner of their own LLC + mechanic at a different shop. Spec 0.2 acceptance is "three orgs → three UIs" which requires per-membership |
| 2026-05-01 | Persona fallback chain: `membership.persona` → `user_profiles.persona` → `'owner'`. Centralized in `lib/persona/config.ts:resolvePersona()` | Old user_profiles.persona (047) still exists; treating it as a fallback means freshly created memberships inherit the user's onboarding persona without needing a backfill |
| 2026-05-01 | `AppContext.setPersona` is now optimistic + auto-POSTs to `/api/me/persona` | Old behavior was localStorage only; spec wants persona to live on the membership row. Wrapping the existing setter avoids touching every call site (AppLayout sidebar toggle, /ask auto-fallback, etc.) |
| 2026-05-01 | `usePersona()` reads from AppContext rather than fetching its own copy | AppContext is already in the tree at the app shell and hydrates on mount; a separate fetch would double-load. Hook returns `persona + config + setPersona + isModuleHidden + homeRoute` so callers don't need to reach into PERSONA_CONFIG manually |
| 2026-05-01 | `shop` persona accepted by DB CHECK + reserved in PERSONA_CONFIG, but no shop-specific UI yet | Spec defines PERSONA_CONFIG.shop for completeness; shop-foreman sidebar/dashboard belongs to Phase 5 (Smart Home Screen). Putting the type system in place now means later sprints just slot in components |
| 2026-05-01 | **Persisted ai_signals + ai_action_cards** instead of the spec's in-memory event bus | Vercel functions are stateless; an in-memory bus would lose signals between requests and across cold starts. Persisting to Supabase means orchestrator survives serverless restarts, and multiple workers / cron jobs can coordinate via the `processed_at` flag |
| 2026-05-01 | Lazy-tick on every /api/ai/inbox GET when there are unprocessed signals | Replaces the spec's "every minute cron" until vercel.json schedule entry is added. Keeps signal-to-card latency tight without burning quota on no-op ticks |
| 2026-05-01 | Two AI tool registries kept side-by-side: existing `lib/ai/tools.ts` (OpenAI function-calling for /api/ask) and new `lib/ai/tool-registry.ts` (Spec 0.3 orchestrator tools) | "Add, don't replace" hard rule — touching the existing /api/ask tools would risk regressions. Convergence happens when /api/ask migrates to the unified registry in a later sprint |
| 2026-05-01 | ActionCard SuggestedAction wraps a `toolCall` so the same action can be triggered by user click, LLM function-call, or rule | One dispatch path through `invokeTool()` means permissions + audit live in one place; UI doesn't need its own action-handler logic |
| 2026-05-01 | dedupe via partial unique index `(organization_id, dedupe_key)` WHERE active | Lets a recurring signal type ("low stock for part X") replace its older active card without filling the inbox with duplicates. Dismissed/resolved cards keep their dedupe_key for audit but free up the slot |
| 2026-05-01 | **Notifications persisted + per-user × per-category × per-channel preferences** | Spec called for an in-memory pipeline; that loses read-state on refresh and can't sync across devices. DB-persisted means bell badge stays consistent across tabs and devices, and channel adapters can be retried independently |
| 2026-05-01 | Generic `reminder_schedules` (062) sits *alongside* the existing aviation-specific `reminders` table (013), not replacing it | "Add, don't replace" hard rule. The aviation reminders carry domain-specific fields (due_hours, priority, snooze) that the generic delivery layer doesn't need. A future sprint can have aviation reminders enqueue generic reminder_schedules for the actual notification fan-out |
| 2026-05-01 | Email/Push/SMS adapters report `delivery_status='skipped'` (not 'failed') with a TODO error message | Spec says these are explicit TODOs (SendGrid/Web Push/Twilio). Marking 'skipped' means the in-app row doesn't lie about a failure that didn't happen, while still logging that the channel intended to fire |
| 2026-05-01 | NotificationBell-only-shows-in-app filter | Bell is the in-app surface; email/push/SMS rows are persisted for audit but live separately. `GET /api/notifications?channel=in-app` is the bell view; non-in-app rows show in any future "delivery log" view |
| 2026-05-01 | Cross-wire from Sprint 0c: urgent/high ActionCards auto-fire notifications via the unified dispatcher | Spec 0.4 says "AI cards" are one of the notification sources. Wiring it server-side means the orchestrator and notification system stay aligned without per-card opt-in. Lower priorities (normal/low) live in the AI Inbox without paging the user |
| 2026-05-01 | `parseOffset()` accepts "30 days before" / "1 day after" / "0 days" / raw "-7" | Spec uses human-readable strings; we parse to signed integers stored in reminder_schedules.offset_days. Idempotent + gives callers flexibility |
| 2026-05-01 | meter_definitions stored as a child table FK'd from meter_readings (not JSONB on meter_profiles) | A reading needs to FK to a *specific* meter so renaming "Tach" doesn't orphan history; child table also gives clean ON DELETE CASCADE. Spec wrote `MeterDef[]` inline on the profile — we split it into a row-per-meter table at the DB level while keeping the API shape (profile + meters[]) the spec's TypeScript expects |
| 2026-05-01 | aircraft.total_time_hours kept untouched; meter_readings is the new source of truth | Sprint 1.1 ships side-by-side with the legacy column. Older surfaces still work; new code uses `getCurrentMeterReading()`. Sunset of total_time_hours is a future cross-cutting cleanup once every consumer migrates |
| 2026-05-01 | Pilot role can write meter_readings (not edit profiles) | Pilots log post-flight readings; that's their job. Profiles + readings *deletes* are mechanic+. Codifies the existing aviation convention without inventing a new permission tier |
| 2026-05-01 | AircraftMeterPanel mounted at /aircraft/[id]/meters sub-route, NOT inside AircraftDetail.tsx | Spec says "embed in AircraftDetail" — but that file is 3,626 lines of legacy redesign code. Cleaner to add a dedicated sub-route now and tab-embed later when the AircraftDetail rewrite happens. Logged as a follow-up |
| 2026-05-01 | Cross-wire 0c → 1.1: POST /api/meter-readings calls emitSignal('meter-reading', ...) async-fire-and-forget | Closes Sprint 0c follow-up. The orchestrator's existing rule (meter-reading-compliance-glance) now consumes real data instead of synthetic harness signals. Failures here do NOT roll back the reading insert (user's primary action succeeds) |
| 2026-05-01 | Whichever-comes-first compute is a *pure function* (lib/compliance/compute.ts) separate from server-side I/O (lib/compliance/recompute.ts) | Lets the compute logic be unit-tested and reused by future client-side previews ("if I log a reading of X, where will I land?") without dragging Supabase into the import graph |
| 2026-05-01 | recompute.ts emits compliance-due signals only on status FLIPS | Spec calls for AI cards on compliance changes; emitting on every recompute would generate one signal per meter reading × per item (noise). FLIP detection: was-actionable-then-vs-now, only emit when `(prev !== overdue && prev !== due-soon) && (now === overdue || now === due-soon)` |
| 2026-05-01 | aircraft.current_hours derived heuristically from a meter named Hobbs/Tach (case-insensitive substring); cycles from one named Cycles | Don't yet have a "primary meter" pointer on meter_profiles. Heuristic is right for ~90% of fleets; explicit per-profile primary-meter config logged as 1.2 follow-up |
| 2026-05-01 | Status `'deferred'` is a *manual override* preserved by recompute | Spec status enum includes deferred; the recompute won't auto-flip a deferred item back to overdue/due-soon until the user explicitly undefer's it. PATCH only accepts deferred or current; due-soon and overdue are owned by recompute |
| 2026-05-01 | linked_work_orders stored as TEXT[] (not FK) on compliance_items | WOs can be deleted independently; a foreign-key cascade would wipe history. Array filtered against work_orders at read time when callers want only-extant-WOs (future) |
| 2026-05-01 | Cross-wire 1.1 → 1.2: POST /api/meter-readings calls recomputeCompliance for that aircraft (after the signal emit) | Spec says "recompute helper that runs on every meter reading insert". TS-side recompute (vs. Postgres trigger) lets us emit AI signals + reach across to the orchestrator with the user's full request context |
| 2026-05-01 | New compliance_items table coexists with aviation reminders (013) and aircraft_ad_applicability (014) | "Add, don't replace". reminders/AD_applicability carry domain-specific fields the generic compliance system doesn't need. A future cross-cutting cleanup can have those tables enqueue compliance_items so we have one source of truth for delivery |
| 2026-05-01 | procedure_sections + procedure_items as child tables (not JSONB on procedures) | inspection_results FKs to a specific procedure_item id; renaming/reordering items doesn't orphan history. Same rationale as meter_definitions in Sprint 1.1 |
| 2026-05-01 | inspection_results.value stored as JSONB | Spec defines value as `string \| boolean \| number` — JSONB lets us store all three without a discriminator column. Frontend reads procedure_item.input_type to interpret. UNIQUE(inspection_id, procedure_item_id) so saving the same row twice upserts |
| 2026-05-01 | procedure_name_snapshot column on inspections | Renames or archives of the parent procedure shouldn't lose context for past inspections. Snapshot captures the name at creation time |
| 2026-05-01 | PATCH /api/procedures/[id] is wholesale-replace when `sections` array is passed (delete + reinsert) | Granular section/item edits are a logged follow-up. v1.3 use cases (typo fixes, item add/remove during template tuning) are well-served by re-saving the whole tree from the builder UI |
| 2026-05-01 | inspections.linked_compliance_items + linked_work_order are stored, but auto-mark wires are deferred | Spec calls for completion to mark linked compliance items complete; that's a 1-2 line cross-wire to /api/compliance-items/[id]/complete from the /complete endpoint, deferred to keep diff size sane. Logged as 1.3 follow-up |
| 2026-05-01 | Photo + signature inputs stub a "Mark recorded" button | DB has photo_urls TEXT[] ready; actual upload needs Supabase Storage wiring + signature pad UI. Rather than gate the whole acceptance criterion on storage, we stub the affordance so the flow ships and the upload pipe drops in later |
| 2026-05-01 | work_order_checklist_items (038) untouched alongside new procedure system | Same "add, don't replace" pattern as 013 reminders + 064 compliance_items. The 038 system is per-WO ad-hoc; the new system is a reusable library with FK'd results. Convergence is a future cross-cutting cleanup |
| 2026-05-01 | Continued items: aircraft_id ON DELETE CASCADE; both work_order FKs ON DELETE SET NULL | Spec is explicit: deferred work follows the AIRCRAFT, not the WO. Cascade on aircraft removes orphan items when an aircraft is deleted; SET NULL on WOs preserves history when an old WO is deleted |
| 2026-05-01 | Pilots cannot write continued_items | Pilot squawks flow through the existing squawk pipeline (013/026); continued_items is a mechanic-triage tool for deferred work. RLS and route both gate writes to mechanic+/admin/owner. Read access is org-wide |
| 2026-05-01 | POST /api/continued-items/[id]/resolve appends notes (with date prefix) instead of replacing | Resolution context is high-value audit. Replacing notes would lose discovery context; appending creates a chronological trail. Date-prefix line keeps the entries scannable |
| 2026-05-01 | PATCH on continued_items auto-stamps resolved_at + resolved_by when status flips to completed/wont-fix | Centralizes resolution metadata so the dedicated /resolve route is for the *interactive* flow (with WO link prompt) but plain PATCH still keeps the row self-consistent. Re-opening (status → open/in-progress) clears the stamps |
| 2026-05-01 | Customer approval public token = 32-char Crockford base32 (~160 bits entropy), generated server-side via crypto.randomBytes | Tokens MUST be unguessable — they're the only auth on the customer-facing route. Crockford alphabet drops I/L/O/U so users typing or reading the URL don't confuse characters. UNIQUE index on approval_requests.public_token guards against (astronomically unlikely) collisions |
| 2026-05-01 | Public customer route lives outside (app)/ route group; APIs use createServiceSupabase() | Customers don't have Supabase auth sessions. Service-role key bypasses RLS while the routes manually validate token shape + request status. Same trust boundary as existing customer_invitations/[token] flow. Public GET refuses drafts (404) and expired (410) — link is dead or unsent |
| 2026-05-01 | New approval_requests table coexists with estimates (040) | Estimates is internal-facing pricing/quoting with no public token; approval_requests is the customer-facing public-token portal with per-line responses. Different shape, different purpose. Per "add, don't replace" |
| 2026-05-01 | /respond is the SINGLE side-effect bus for customer actions | One endpoint owns: (a) update line response, (b) optionally create continued_item via 1.4 cross-wire (idempotent via FK), (c) recompute parent status, (d) fire 0d notification to operator. Centralizing keeps the cross-wire chain auditable in one place |
| 2026-05-01 | approval_line_items.resulting_continued_item FK provides idempotency for the 1.5 → 1.4 cross-wire | A customer who flips approve → defer → approve → defer wouldn't otherwise create N continued_items. The FK lets the respond route check "did I already make one?" before insert. If response flips AWAY from deferred, we leave the existing continued_item alone — it has its own lifecycle |
| 2026-05-01 | "Copy link" affordance on operator-side ApprovalDetail | Until SendGrid integration lands (same pending follow-up as 0d notifications), operators paste the URL into email/text manually. The /send endpoint returns the public_url so the UI can show + copy it without re-deriving |
| 2026-05-01 | inventory_parts coexists with parts_searches/part_offers (021) | "Add, don't replace". 021 is the EXTERNAL Atlas catalog (Google Shopping, eBay scrape); inventory_parts is the operator's LOCAL stock. Spec note about returning inventory results before catalog in searchParts is a logged 2.1 follow-up |
| 2026-05-01 | work_order_lines.inventory_part_id is NULLABLE | The legacy column work_order_lines.part_number TEXT (016) still works for one-off external parts. The new FK is the bridge; auto-decrement on WO line install is a logged follow-up that needs touching the legacy WO API |
| 2026-05-01 | consumeInventoryPart() clamps at zero + returns shortfall (does not throw) | DB CHECK on qty_on_hand >= 0 makes hard-failure too noisy for a real shop ("we already used the last one"). Soft-clamp + shortfall-in-result lets the UI surface a warning toast and the operator decides — restock first, or accept the row |
| 2026-05-01 | Latest-cost heuristic on restock unit_cost_override (not weighted-average) | Weighted-average needs Σ(qty * cost) / total_qty bookkeeping per restock. Latest-cost is good enough for v0 and matches what most small shops actually do. WAC is a logged follow-up |
| 2026-05-01 | po_number is per-org-per-year sequence ("PO-2026-0001") with UNIQUE(org, po_number) safety net | Allocation is two queries (read max + insert with conflict retry); for v0 fleet sizes that's fine. A dedicated po_number_sequences table or pg sequence is the production-grade move |
| 2026-05-01 | low-stock signal payload carries alert_emails | Sprint 0d's email adapter is stubbed pending SendGrid. Carrying the email list on the signal means the cross-wire fires correctly when delivery lands — no schema change needed at that point |
| 2026-05-01 | RESTRICT FK from purchase_order_lines.inventory_part_id to inventory_parts | Hard-deleting a part referenced by past POs would orphan PO history. RESTRICT forces the operator to archive (is_archived=true) instead — clean from view, history preserved |
| 2026-05-01 | Vendors get NULLABLE vendor_id ALTERs alongside legacy `vendor TEXT` columns on inventory_parts/purchase_orders/work_order_lines | Per "add, don't replace": existing rows referencing vendors as free-text strings keep working. New writes can carry vendor_id. UI prefers vendor_id when present; future cleanup can fold the strings via a name-match helper |
| 2026-05-01 | vendors FK is ON DELETE SET NULL on all three referrer tables | Hard-deleting a vendor preserves history (the parts/POs/WO lines stay; their vendor_id becomes NULL and the legacy `vendor TEXT` keeps the name). Archive (is_archived=true) is the recommended path |
| 2026-05-01 | UNIQUE(organization_id, name) on vendors | Different orgs can each have their own "Aircraft Spruce" entry — vendor names aren't globally unique |
| 2026-05-01 | List API computes per-vendor usage counts via 3 cheap aggregate queries (parts/POs/WO lines) | A JSON aggregate / view would be cleaner long-term but the count-per-vendor join cost is trivial at v0 fleet sizes. Each query is `select vendor_id where vendor_id IN (...)` and we group in JS |
| 2026-05-01 | Approved-vendor flag is display-only in 2.2 | Spec defines the field for "approved-vendor-only" enforcement. Future WO-line policy can refuse outside_service rows where vendor.approved=false (or warn + soft-allow). Keeping it display-only for v0 means we don't trip on legacy WOs that pre-date a vendor's approval flow |
| 2026-05-01 | Partial UNIQUE index on time_entries(technician_id) WHERE end_time IS NULL | "One open entry per technician" is a hard invariant — the running-timer chip on Topbar would lie if a tech had two open rows. Index is the safety net; clockIn() helper checks first to give a friendly 409 instead of letting the constraint trip the user with a postgres error |
| 2026-05-01 | hourly_rate is FROZEN at clock-in (not retro-rewritten) | A later rate change must NOT rewrite cost on entries that have already been billed or submitted. The rate column on time_entries captures the rate-at-time-of-clock-in; org-level rate changes only affect future entries |
| 2026-05-01 | RLS allows org-wide READ but writes are gated to self (or owner/admin for anyone) | Mechanics need to see each other's entries for a "currently working" board; only the person on the clock should be able to start/stop their own entry. Owners + admins can correct anyone's entries (for forgot-to-clock-out fixes) |
| 2026-05-01 | RunningTimerChip ticks once a second when an open entry exists, no tick otherwise | Saves a setInterval when nobody's on the clock. The polling loop (30s) keeps the chip in sync across tabs / devices |
| 2026-05-01 | aggregateWorkOrderHours sums BOTH time_entries AND work_order_lines.hours/rate (016) | New time-clock entries coexist with manual labor lines per "add, don't replace". The aggregate function is the single source of truth for WO labor totals; legacy code paths that read work_order_lines.line_total directly continue to work, just under-counting time-clock-driven labor |
| 2026-05-01 | Per-WO time-clock UI mounted at /work-orders/[id]/time-clock sub-route | Spec calls for embed inside legacy WorkOrderPanel (1k+ LOC). Following the established pattern (Sprints 1.1/1.2/1.3/1.4/1.5/2.1) we sub-route now, tab-embed later. /time-clock org-wide page provides a "currently running" board across the org |
| 2026-05-01 | Multi-view system is a generic shared library + per-module config registry | The spec expects this to apply across 8+ modules. Hardcoding view logic per page would 8x the code. ModuleViewShell + MODULE_CONFIGS lets each module declare fields/groupable/dateField in one place; the shell does the rest |
| 2026-05-01 | Wired the multi-view shell into ONE module (inspections) this sprint; remaining 9 are follow-ups | Each module is a separate small drop-in (add MODULE_CONFIGS entry + 1-line `<ModuleViewShell module="…">` in the page). Doing all 9 in one sprint risks regressions on legacy WO/invoices/logbook surfaces; one wire-in proves the system end-to-end and de-risks the bulk migration |
| 2026-05-01 | applySavedView is a PURE function (no DB I/O) | Lets the same logic run server-side (future: pre-filter via SQL) or client-side. Keeps view logic testable. Filters: string equality / array any-of / numeric {min,max}. Sort: field+direction. groupBy + display_config are also pure attributes the views consume |
| 2026-05-01 | Active view selection persists in sessionStorage per module | Spec acceptance: "saves a custom filtered view, picks it later." Persisting in DB (which is what the SAVE part does) is half — the UX expectation is that navigating away and back doesn't reset to the default. sessionStorage matches what every modern data tool does (Linear, Asana, etc.) |
| 2026-05-01 | Seed views are CLIENT-SIDE virtual rows (not DB-seeded per-org) | Seeding a row in saved_views per-org-on-create would require a hook on org creation + invalidation when MODULE_CONFIGS changes. Synthesizing seed views from MODULE_CONFIGS at render time means a config change ships seamlessly; the cost is that operators can't delete a seeded view (only "Save as" to create a persisted copy and edit/delete that one) |
| 2026-05-01 | Default-view-clear scoped per (user OR org-shared) | Setting is_default=true on a personal view clears the prior personal default but doesn't touch the org-shared default — and vice versa. The two scopes have independent defaults; the active-view resolver picks personal-default → org-default → first-seed → fallback |
| 2026-05-02 | tools.next_calibration_date denormalized from latest calibration_events row via AFTER INSERT trigger | Calibration Due query is the hot path (Dashboard widget, due-list view, WO save guard). A LATERAL JOIN to "max(performed_at) per tool" on every read would be expensive at scale. Trigger keeps the denorm authoritative + cheap range scan |
| 2026-05-02 | tolerance_days extends the deadline before assertToolCalibrated() flips to overdue | Real shops give a 7-14 day grace post-due-date. Hard-flipping at midnight on next_calibration_date frustrates mechanics in the middle of jobs. Tolerance is per-tool — the IA's torque wrench can have 0-day tolerance, the borescope can have 14 |
| 2026-05-02 | work_order_tool_uses.was_overdue is a permanent audit field set at INSERT time | Even if the tool is later recalibrated, the audit row tells the truth about what was used when. An IA reviewing the WO 6 months later sees "torque wrench was overdue when used" with the original timestamp. assertToolCalibrated runs server-side at the POST so client-side bypass is impossible without admin force= override |
| 2026-05-02 | checkOutTool() runs the same calibration guard as the WO add | An overdue tool shouldn't even leave the crib. Single source of truth for the rule (assertToolCalibrated) means policy changes propagate everywhere |
| 2026-05-02 | UNIQUE(organization_id, serial_number) on tools | Two orgs commonly have a "TW-001" — generic numbering. Per-org uniqueness mirrors vendors (UNIQUE(org, name)) and inventory_parts patterns from prior sprints |
| 2026-05-02 | clock_events is the WORKDAY; sprint 2.3's time_entries is the per-WO labor. Bridge via time_entries.clock_event_id FK | Spec 2.5.3 acceptance is "per-WO entries roll up INSIDE the daily clock." Without the FK we'd have to time-window-match (start_time inside clock_in_at..clock_out_at) which breaks for split shifts / midnight crossings. Explicit FK = one query, no heuristics |
| 2026-05-02 | One open ClockEvent per employee enforced at the DB via partial UNIQUE | Mirrors sprint 2.3's pattern. App-side getCurrentClockState() check first for friendly error; partial UNIQUE catches the race between two concurrent POSTs |
| 2026-05-02 | Sprint 2.3 clockIn() lookup of open ClockEvent is best-effort, not blocking | Daily clock is OPTIONAL — orgs running just per-WO labor (no shop attendance) shouldn't need to clock-in daily before clocking into a WO. If lookup fails or there's no open clock, time_entries.clock_event_id stays NULL and per-WO timing works unchanged. "Add, don't replace" preserves the older flow |
| 2026-05-02 | clockOutEmployee() bulk-closes ALL of this employee's open time_entries, not just those linked to this clock_event_id | Defensive: if a tech somehow has open per-WO entries from a previous (unclosed) ClockEvent, we don't want them dangling forever. The total cost of an extra `WHERE end_time IS NULL AND technician_id = X` cleanup is one round-trip; closed_time_entries count is returned for the UX toast |
| 2026-05-02 | Breaks stored as JSONB array on the ClockEvent row, not a child table | Bounded list (3-5 breaks per workday max). Same rationale as sprint 2.5.1's Shift.checklist. status guards via `eq('status', 'on-break')` on the END-break update prevent concurrent races |
| 2026-05-02 | Time-off decision goes through dedicated POST /api/time-off-requests/[id]/decide rather than generic PATCH | The status-flip is a meaningfully different operation (audit fields decided_by/decided_at + manager_comment + atomic guard) from "edit my own pending request fields". Generic PATCH stays for cancellation / requester edits; /decide centralizes the approval contract |
| 2026-05-02 | Atomic eq('status','pending') filter on the decide UPDATE | Two managers simultaneously hit Approve → without the filter, second write silently overwrites first decision. With the filter, only one row matches; the other returns zero-rows-affected and surfaces as 409. No DB lock needed |
| 2026-05-02 | Default new request status='pending' (skip 'draft' for v0) | Spec keeps 'draft' in the enum (kept for forward-compat) but the practical UX is "submit goes to pending." Saves an extra "publish your draft" click. Admins submitting on behalf can pass status='draft' explicitly if they're scaffolding a request |
| 2026-05-02 | PTO bar overlay rendered BEFORE shift bars in each calendar cell | Visually the "blocked day" should pre-empt the colored shift entry. Same `<ul>`, gray block above colored block. Bounded slice (2 in month view, 6 in week view) so heavy-PTO orgs don't blow out the cell |
| 2026-05-02 | shifts is the PLAN, time_entries (sprint 2.3) is the ACTUAL — they coexist, neither replaces the other | Spec 2.5.1 acceptance is "the WO assignee picker reflects who's on shift now" — that's a plan question, not a clock-in question. A tech who clocked in but isn't on shift is a separate (legitimate) signal. Keeping them as distinct tables means each can evolve independently |
| 2026-05-02 | shifts RLS: any org member READs, only owner/admin WRITEs | Mirrors time_entries (sprint 2.3) read pattern — the team needs to see the schedule even if it's another tech's shift. WRITE is admin-only because techs don't self-schedule; swap requests go through shift_covers, which has its own RLS allowing tech-self-insert |
| 2026-05-02 | One-live-cover-per-shift via partial UNIQUE on (original_shift_id) WHERE status IN ('open','claimed') | Prevents the "tech spam-creates 5 cover requests" pathology. If a tech changes their mind, they edit the existing request. The partial WHERE means closed (approved/rejected) covers don't block re-requesting if a future need arises |
| 2026-05-02 | Claim race resolved at the app layer via `eq('status','open')` filter on the UPDATE | Two techs simultaneously click "I'll cover this" on an open request. Without the filter, the second write would succeed and overwrite the first. With `eq('status','open')` only the first UPDATE matches a row; the second sees zero-rows-affected and surfaces as 409. No DB lock needed |
| 2026-05-02 | Approve transition is two writes (cover row + shift row) sequentially with best-effort rollback | Supabase JS doesn't expose Pg transactions. PATCH cover→approved succeeds, then UPDATE shifts (technician_id + status='swapped'); on the second write failing we revert the cover back to 'claimed'. Future stored proc `approve_shift_cover()` for true atomicity is logged follow-up |
| 2026-05-02 | Shift.reminders + Shift.checklist as JSONB rather than child tables | Bounded-size lists (typical < 20 items each); JSONB matches the inspection_results pattern from 1.3. Reminders cross-wire to sprint 0d's reminder_schedules can evolve (eager vs lazy enqueue) without a schema change. Granular checklist edits are a follow-up |
| 2026-05-02 | /scheduler renders a dedicated ShiftCalendar instead of the generic ModuleViewShell | Spec 2.5.1 calls for "default view: ShiftCalendar — Month or Week" with multi-shift-per-day rendering, status-tinted tiles, and time-range labels. The 2.4 generic CalendarView shows one event per day with a primary-field label — too thin for a shift schedule. The shifts MODULE_CONFIG is registered (with calendar default + 5 seeded views) so the generic shell can be wired in later if a saved-views surface for shifts is wanted |
| 2026-05-02 | TodaysShiftsWidget built as a standalone embeddable component, NOT mounted on Dashboard.tsx this sprint | Mirrors the AircraftMeterPanel / AircraftCompliancePanel deferral pattern from sprints 1.1 / 1.2. The 703-line legacy Dashboard layout needs surgical insertion at the right grid slot best done with the operator. Component is drop-in ready (self-contained fetch, no parent props required) |
| 2026-05-02 | shifts MODULE_CONFIG defaults `view_type='calendar'` instead of `'list'` | Every other module in MODULE_CONFIGS defaults to list. Shifts is fundamentally a time-grid concept — opening /scheduler with a vertical list would feel wrong. The other 4 seeded views (this-week, board-by-status, board-by-tech, table) cover the remaining shapes |
| 2026-05-02 | Document expirations: ALTER existing `documents` table instead of new table | Per "add, don't replace": the row IS the document — same FK targets (aircraft, organization), same RLS, same lifecycle. The new fields are additive (10 NULLABLE / DEFAULT-safe ALTERs). Spec 2.6.2 acceptance is "register doc with expiration" not "register expiration separately" — folding into documents matches the user's mental model and means the existing /documents views automatically pick up the new fields |
| 2026-05-02 | reminder_offsets stored as JSONB on the document row (not a child reminders table) | Bounded list (3-5 reminders per doc max). The cross-wire to sprint 0d's reminder_schedules is the actual source of truth for delivery — the JSONB on the doc is just the user's preference template that gets re-applied on every edit. Mirrors sprint 2.5.1 Shift.reminders pattern |
| 2026-05-02 | expiration_status both stored AND recomputed on read | Stored value backs the partial index `documents_expiration_status_idx` for "WHERE status='expiring-soon'" hot-path queries. Recomputed value at read time is for accuracy when the row hasn't been touched in days. The pure helper `recomputeExpirationStatus()` in lib/documents/expiration.ts is the single source of truth — both the API and any future cron can call it |
| 2026-05-02 | Idempotent re-enqueue on PATCH /api/documents/[id]/expiration via DELETE-then-INSERT into reminder_schedules | A user editing the expiration date and re-saving must not pile up duplicate reminders. Wiping all rows for `entity_kind='documents' AND entity_id=X` and re-inserting from scratch is the simplest correctness model. RLS scopes WRITE to user_id=auth.uid() so we use the service client to delete + insert fan-out rows (user_id NULL) |
| 2026-05-02 | reminder_schedules.user_id NULL = fan-out at fire time | Sprint 0d's tick processor already handles user_id NULL by querying accepted org members at fire time. Enqueueing with NULL means we don't need to know recipients at insert time (which is good — they may join the org between insert and fire). Per-recipient overrides via notification_preferences still apply at delivery |
| 2026-05-02 | Per-aircraft documents sub-route reuses ExpiringDocsView with `aircraftId` prop instead of building a parallel panel | The shape is identical (list + filter + form) — only the GET filter changes. Reusing the same component cuts the surface area in half and means UX changes propagate to both surfaces. Same DRY pattern as the compliance panel reuse in sprint 1.2 |
| 2026-05-02 | OWNER/MECHANIC/SHOP_DOC_CATEGORIES are seeded constants in types/index.ts (not a DB lookup table) | Categories are aviation-domain stable (FAA cert types, insurance kinds, etc.) and the spec ships with explicit lists. Hardcoding in types/index.ts means the form's persona-aware dropdown swaps lists with no DB roundtrip. Custom categories are still allowed via "Other" + the free-text field on the doc row |
| 2026-05-02 | Phase 4 entered with 4.3 first instead of 4.1 | Spec 4.1 (Airbly) and 4.2 (FSP) need partner OAuth credentials; 4.3 (ADSB Exchange) only needs a RapidAPI key which is already provisioned. Building 4.3 first proves the telemetry pipeline + Hobbs/Tach inference math end-to-end, so when 4.1/4.2 unblock they only need to layer their per-source clients onto the existing flight_events/telemetry_sources schema |
| 2026-05-02 | flight_events is a new entity (not an extension of meter_readings or reminders) | A flight is a first-class concept — it has start/end/path/origin/destination/confirmation lifecycle that doesn't fit either of those tables. meter_readings.source_flight_event_id FK bridges the two so an inferred reading carries provenance back to the flight that generated it. Same "add, don't replace" pattern as 2.6.2 documents.expiration extension vs a parallel expirations table |
| 2026-05-02 | telemetry_sources is per-aircraft per-source (one row per pair) instead of a JSONB column on aircraft | Spec 4.4's multi-source priority + dedupe logic needs to query "all sources enabled for this aircraft, sorted by priority." A child table makes that an indexed read; JSONB on the parent would need a function-based index per source. Same shape as compliance_items / meter_definitions (sprint 1.1). The unique (aircraft_id, source) constraint guarantees we never double-provision |
| 2026-05-02 | meter_readings.source CHECK widened to include telemetry sources rather than dropping the constraint | Per "add, don't replace": the legacy values ('manual','automatic','imported') from sprint 1.1 still mean what they meant; we ADDED the four telemetry sources alongside. Old rows from 1.1 keep parsing in TS via the widened MeterReadingSource union. UI badge logic (badgeLabel + tierForConfidence) reads the wider field |
| 2026-05-02 | Confidence stored on both flight_events AND meter_readings, intentionally duplicated | flight_events.confidence is the source-of-truth at detection time. meter_readings.confidence is propagated from there at insert time so downstream consumers (dashboard widgets, compliance recompute) don't have to JOIN to render the badge. On confirm/override, both rows are mutated together by /api/flight-events/[id]/confirm |
| 2026-05-02 | recent_pings buffer lives in telemetry_sources.config JSONB, not a separate table | Bounded list (~24h × 2000 cap = 50KB JSON max). Same rationale as Shift.checklist (2.5.1) and reminder_offsets (2.6.2). A child telemetry_pings table would add an indexed write per ping (every 5 min × every aircraft) for a use case where the only consumer is flightDetector inside the same sync route. Trade-off accepted; if we hit JSON-row-too-big in future, easy migration to a child table |
| 2026-05-02 | Confidence band per source × ping density factor inside it | Spec 4.3 gives a single range per source (0.55-0.75 for ADSB inferred). We make the score non-uniform within that band: dense pings (>50% of expected per minute) → 0.75; sparse (<10%) → 0.55. Linearly interpolated between. Means the "Estimated" badge on a 1.4 hr flight with 84 pings carries a different score than the same flight with 12 pings, even though both render the same amber badge |
| 2026-05-02 | First inferred reading for a meter is SKIPPED — no fabricated absolute baseline | meter_readings.value is an absolute, not a delta. We need a starting point. If the aircraft has zero prior readings for the Hobbs meter, ADSB inference can't fabricate "your aircraft now reads 1234.5 hrs" because we don't know what it reads. We skip the meter_readings insert (the flight_event still lands with the inferred delta) and surface the gap on the Sync tab so the owner enters their first reading manually. After that, deltas accumulate |
| 2026-05-02 | Cron schedule 5-min for /api/integrations/adsb/sync, capped to 50 aircraft per tick | Vercel function timeout is 60s on Hobby/Pro. RapidAPI calls are 8s timeout each. 50 aircraft × ~1s avg (most return immediately from a small response) fits comfortably. Cron runs every 5 min so a fleet of 50 syncs every ~5 min; a fleet of 500 takes ~50 min between syncs per aircraft, which is well within the spec's "within 30 minutes" acceptance for a single aircraft (it's the time-since-takeoff that matters, not time-since-cron) |
| 2026-05-02 | RAPIDAPI_ADSB_EXCHANGE_KEY only read inside the client factory; never logged | NEVER returned in any API response, NEVER bundled into client code (lib/telemetry/sources/adsb-exchange.ts is server-only). getAdsbExchangeClient() throws if the env var is missing rather than no-op-ing — silent failure would have caused the same "stale build" pain we just hit with Vercel deploy. Documented in .env.local.example as server-only |
| 2026-05-02 | Phase 5 entered with 5.1 first; Phase 4 partial (4.3 only) | 4.1 / 4.2 / 4.4 still need partner credentials. 5.1 only depends on the ai_signals + ai_action_cards tables from 0c, ActionCard component already built — so it's free to ship. Doing 5.1 first proves the persona-aware Smart Home shell, action card stack, proactive card generators, and aircraft tile pattern, so Phase 5.2-5.8 only need to layer richer card sources (LLM, ML predictions) onto an established surface |
| 2026-05-02 | SmartHome reuses existing AI Inbox primitives — no new ai_action_cards table changes | The 0c schema already has persona/priority/category/dedupe_key/source/confidence — exactly what 5.1 needs. ActionCardStack just imports useAIInbox + ActionCard from /components/ai/. Means /(app)/inbox and /(app)/my-aircraft and /(app)/my-day all render the SAME cards filtered differently. One source of truth, three surfaces |
| 2026-05-02 | Proactive card generation runs on every page render, not as a cron | Cheap bounded scans (max 50 aircraft, 20 docs, 20 compliance, 20 tools, 20 WOs) — likely <500ms total even on a busy org. Idempotent via dedupe_key partial unique index. A cron version is more correct long-term (logged 5.1 follow-up) but adds operational complexity (cron failure mode, tick latency, etc.) without much v0 benefit. Per-render scans match how /aircraft/[id]/sync triggers ADSB syncs lazily on click in 4.3 — same "lazy + idempotent" pattern |
| 2026-05-02 | Owner card scan filters expiring docs by `target_persona` is intentionally NOT applied | The ownerCards function scans all `documents.has_expiration` rows regardless of target_persona — owner persona should see ALL upcoming expirations whether they're owner-targeted or mechanic-targeted, because owners care about the fleet's overall expiration health. mechanicCards filters separately. If an org wants the owner persona to ONLY see owner-targeted docs, that's an `additional` filter to add (not a baseline) |
| 2026-05-02 | Existing /(app)/my-aircraft/page.tsx rewritten to mount SmartHome | The legacy MyAircraftClient was a per-user-filtered aircraft list — duplicative with /aircraft (which is the canonical aircraft list with role filtering). Per Spec 5.1 explicit instruction "/my-aircraft is owner home", the route was repurposed. The legacy MyAircraftClient component file stays on disk for one cycle in case any other route imports it — can be deleted in a follow-up cleanup commit |
| 2026-05-02 | Smart Home greeting copy is deterministic (time-of-day + persona-specific status sentence) | LLM-generated personalization is the upgrade path (Spec 5.4 Voice/Camera + a future "AI Greeter" sprint). v0 ships a hand-coded subline keyed on persona to prove the persona-aware pattern; the LLM swap-in only changes the function body, not the React structure. Means we can ship 5.1 without ANTHROPIC_API_KEY in the loop |
| 2026-05-02 | AppLayout owner nav top item flipped Dashboard → "Home" pointing at /my-aircraft | Spec 5.1 calls for SmartHome to *replace* the Dashboard. We keep /dashboard mounted for now (it's still in middleware + has a working page); the nav top item just no longer points at it. Phase 5.x can retire /dashboard entirely when no widget needs surgery. PERSONA_CONFIG.homeRoute swap means the org-switch redirect from sprint 0a now lands users on Smart Home, not the legacy stat-card surface |
| 2026-05-03 | First production LLM use lands with sprint 5.6, not 5.1 | 5.1 deliberately shipped without LLM (rule-based card generation only) so the persona-aware Smart Home shell could deploy without ANTHROPIC_API_KEY. 5.6's explainer is the lowest-risk, highest-leverage place to introduce the Anthropic dependency: customer-facing surface where graceful degradation already exists (operator's description as fallback), bounded cost (max 50 lines × ~$0.005 = $0.25 per approval), and the failure mode is "customer reads technical text instead of plain English" — never a broken page |
| 2026-05-03 | lib/ai/anthropic.ts uses raw fetch() instead of @anthropic-ai/sdk | One less dependency to update + version-pin. The Messages API surface we use is small (system + user + max_tokens + temperature). When/if we adopt streaming or tool-use we'll likely add the SDK then; for the explainer + ask use case raw fetch is enough and keeps the bundle clean |
| 2026-05-03 | ai_activity_log writes happen on every LLM call (success AND failure) | Foundation for org-level cost dashboards (5.6 follow-up), debugging stuck calls, and detecting cost-cap-exceeded events. Wrote the table + RLS now even though no admin UI consumes it yet — schema-first means future sprints don't have to migrate retroactively. Service role bypasses RLS for the writes (the LLM client is server-only), and the org-read RLS exposes the rows to in-org users for the future dashboard |
| 2026-05-03 | Cost cap of 50 line items per approval, not per ai_activity_log call | A 50-line approval is unusual — most are 3-15 lines. The cap tells the operator "you're out of the AI explainer's design envelope, customers will see your technical descriptions" rather than silently spending more. cap-exceeded is a distinct status in the log so admin dashboards can flag operators who keep hitting it (could mean their workflow needs a different tool, or the cap should rise) |
| 2026-05-03 | Per-line failure tolerance, not per-approval | If line #3's LLM call 5xx's, lines #1 and #2 still land. Customer view's existing description fallback covers the failed line transparently. The alternative (all-or-nothing) would mean a transient Anthropic outage silently nuked the AI for the whole approval — worse UX |
| 2026-05-03 | Customer-side Ask handler is rate-limited via row count on ai_activity_log, not a separate rate-limit table | One less table. ai_activity_log already has the per-call audit trail; counting rows in the past hour for (org, scope='approval-customer-ask', entity_id=approval_id) is the rate limit. Idempotent across deploy boundaries because the log persists. 5 questions/hour per token is generous enough for legitimate curiosity, tight enough to bound cost on a single token |
| 2026-05-03 | LLM hard constraints baked into SYSTEM_PROMPT, not post-hoc validation | "Don't invent FAR numbers" is a string the model sees, not a regex we run on output. Cheaper, simpler, and the model trained to follow these instructions handles the long tail (e.g. "don't make up SB references", "don't promise turnaround times") without us writing N validators. Trade-off accepted: occasional system-prompt drift; mitigated by surfacing every call in ai_activity_log so we can spot regressions |
| 2026-05-03 | answerCustomerQuestion grounds in approval line items only, not the full WO | Customer doesn't have access to the WO; they have the approval link. Bringing more context (e.g. full work_order.scope, prior compliance history) risks leaking org-internal data to a public token. The line items the customer is already reading are the safe context. Future: if/when we want richer answers, add a per-org policy toggle "include WO scope in customer Ask answers" |
| 2026-05-02 | Added Phase 7 — Aircraft Operating Economics — 8 features. Differentiator: only platform combining MRO software with owner-facing operating economics + AI receipt extraction + tax-time P&L. | Owner-facing P&L answers "am I making money?" from real data. AI receipt extraction (Claude Vision via lib/ai/anthropic.ts from 5.6) eliminates manual data entry — forward bills, AI structures them. True per-hour cost calculator amortizes scheduled reserves + annual fixed + loan + depreciation across actual flight hours from flight_events (4.3). Tax-time P&L PDF replaces $500-2000 accountant work with one click. Source priority framework generalizes 4.3's confidence-scoring system-wide. Inserted before Phase 3 (commercial layer) in build order — owners care about Phase 7 more than per-aircraft contract pricing. Note: spec text inside Phase 7 references migrations 078-081 — those numbers are already taken (078 was used for 5.6 smart_approvals); when the Phase 7 sprints actually run, migrations will be renumbered to whatever's next. |
| 2026-05-03 | AI Inspector emits a NEW category 'audit-finding' rather than reusing 'anomaly' or 'insight' | Audits are a distinct semantic class — they're "this work was already done; here's what's missing/wrong" rather than "a thing happened" (anomaly) or "a thing is true" (insight). A separate category lets the AI Inbox filter "show me audit findings only" cleanly + future Phase 5.2 grouping work has a stable key to bucket on. The widening (migration 079) is additive; existing rows in other categories are unaffected |
| 2026-05-03 | wo-auditor combines deterministic rule checks with optional LLM body composition | The 7 rule checks are pure data reads — they always run, always work, no LLM cost or risk of hallucination on safety-relevant data ("this RII is missing"). The LLM is ONLY used to compose the 1-2 sentence card body summary; if ANTHROPIC_API_KEY is missing or the call fails, a deterministic English template kicks in. Means a sysadmin can disable LLM cost entirely by unsetting the key and audits still work — they just get less narrative |
| 2026-05-03 | Background-fire on WO status close uses dynamic import inside void async IIFE | Two reasons: (1) the audit code path doesn't need to be in every WO PATCH bundle slice — dynamic import keeps it lazy; (2) the void IIFE makes the non-blocking semantics explicit — the response returns 200 to the user immediately while the audit runs server-side. Failure logs to console.warn, never propagates. Same pattern as 5.6's regenerateForApprovalRequest auto-fire on /send |
| 2026-05-03 | Cron at */15 min for /api/cron/wo-audit, not */5 like ADSB sync | Audits aren't time-critical — a finding 15 min after WO close vs 5 min isn't a meaningfully different UX. Less aggressive cadence saves Vercel function invocations + Anthropic spend. Cron is a backstop for hook misses (direct DB updates, hook errors), not the primary path |
| 2026-05-03 | Idempotent audit cards via dedupe_key=`wo-audit:<wo_id>` | Same pattern as 5.1's proactive card generators. Re-running auditWorkOrder updates body/evidence/priority on the existing dedupe-keyed active card rather than creating a duplicate. Operator fixes a finding → re-audits → card refreshes in place; if all findings clear, the card flips to the "audit clear" insight at priority='low'. User can dismiss to remove from the inbox |
```
