# Prompt Archive

This folder stores high-signal product prompts, architecture notes, and implementation batches that were actively used to drive repo changes.

Guidelines:
- Keep the original source markdowns in place when they already exist elsewhere in the repo.
- Add a dated index file here that summarizes the prompt batch, the requested outcomes, and the source files used.
- Prefer linking to existing source markdowns instead of duplicating large prompt files.

